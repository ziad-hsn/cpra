# SDK Go declarations

Generated by `python3 scripts/sdk/reference.py`. Do not edit generated tables.

These are the SDK candidate contracts. Server coverage is being implemented and qualified by operation. Consult server discovery and the implementation status for supported routes; worker execution remains a separate build and release contract. A listed SDK method is not evidence of a deployed server route. See the [guide](index.md) for runnable local examples.

Schema input SHA-256: `dfa665889150cf0f57c0718ccca220c5c39d6b77032c47767778be604204f49a`.

This index includes exported types, constants, errors, functions, and methods from the public SDK packages. Signatures and comments are extracted from Go syntax. Function bodies and private fields are omitted. Platform-specific implementation files may produce repeated signatures.

The combined `api` listing includes base types plus the tagged extension. Consult [wire types](wire-types.md) for the per-type build boundary; `api` declarations that also exist in the base projection are available without `externaljobs`.

## Package cpra

### *ActionsService.Get

Get calls GET /api/v2/actions/{id}.

```go
func (s *ActionsService) Get(ctx context.Context, id string) (*Response[api.Action], error)
```

### *ActionsService.Iterate

Iterate walks pages lazily.

```go
func (s *ActionsService) Iterate(opts ListOptions) *Iterator[api.Action]
```

### *ActionsService.List

List calls GET /api/v2/actions.

```go
func (s *ActionsService) List(ctx context.Context, opts ListOptions) (*Response[api.ActionList], error)
```

### *ActionsService.Review

Review calls POST /api/v2/actions/{id}/review.

```go
func (s *ActionsService) Review(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Action], error)
```

### *AmbiguousError.Error

```go
func (e *AmbiguousError) Error() string
```

### *AmbiguousError.Is

```go
func (e *AmbiguousError) Is(target error) bool
```

### *AmbiguousError.Unwrap

```go
func (e *AmbiguousError) Unwrap() error
```

### *Client.Access

Access returns the authenticated principal and explicit effective permissions.
Resource discovery reports supported operations and does not grant permission.

```go
func (c *Client) Access(ctx context.Context) (*Response[api.AccessInfo], error)
```

### *Client.Capabilities

Capabilities calls GET /api/v2/discovery.

```go
func (c *Client) Capabilities(ctx context.Context) (*Response[api.Capabilities], error)
```

### *Client.CloseIdleConnections

CloseIdleConnections releases idle HTTP connections; it does not cancel calls.
CloseIdleConnections closes idle transport connections without interrupting
requests already in flight. It does not cancel operations on the server.

```go
func (c *Client) CloseIdleConnections()
```

### *Client.Explain

Explain calls GET /api/v2/schemas/{id}.

```go
func (c *Client) Explain(ctx context.Context, id string) (*Response[api.Explanation], error)
```

### *Client.History

History calls GET /api/v2/history.

```go
func (c *Client) History(ctx context.Context, opts ListOptions) (*Response[api.EventList], error)
```

### *Client.JobTypes

Build requirement: `externaljobs`.

JobTypes manages immutable versioned descriptors; it never loads executable code.

```go
func (c *Client) JobTypes() *JobTypesService
```

### *Client.Live

Live calls GET /api/v2/healthz.

```go
func (c *Client) Live(ctx context.Context) (*Response[api.Health], error)
```

### *Client.Metrics

Metrics returns the structured diagnostic snapshot.

```go
func (c *Client) Metrics(ctx context.Context) (*Response[api.Metrics], error)
```

### *Client.Prometheus

Prometheus streams the metrics representation without accumulating it in memory.
The bound detects excess data rather than silently returning a truncated success.

```go
func (c *Client) Prometheus(ctx context.Context, w io.Writer) error
```

### *Client.Ready

Ready calls GET /api/v2/readyz.

```go
func (c *Client) Ready(ctx context.Context) (*Response[api.Health], error)
```

### *Client.RuntimeConfig

RuntimeConfig calls GET /api/v2/config.

```go
func (c *Client) RuntimeConfig(ctx context.Context) (*Response[api.RuntimeConfig], error)
```

### *Client.State

State calls GET /api/v2/state.

```go
func (c *Client) State(ctx context.Context) (*Response[api.State], error)
```

### *Client.Version

Version calls GET /api/v2/version.

```go
func (c *Client) Version(ctx context.Context) (*Response[api.Version], error)
```

### *Client.Workers

Build requirement: `externaljobs`.

Workers calls GET /api/v2/external-workers.

```go
func (c *Client) Workers(ctx context.Context, opts ListOptions) (*Response[api.WorkerObservationList], error)
```

### *CredentialsService.Create

Create calls POST /api/v2/credentials.

```go
func (s *CredentialsService) Create(ctx context.Context, req api.Credential) (*Response[api.Credential], error)
```

### *CredentialsService.Delete

Delete calls DELETE /api/v2/credentials/{id}.

```go
func (s *CredentialsService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *CredentialsService.Get

Get calls GET /api/v2/credentials/{id}.

```go
func (s *CredentialsService) Get(ctx context.Context, id string) (*Response[api.Credential], error)
```

### *CredentialsService.Iterate

Iterate walks pages lazily.

```go
func (s *CredentialsService) Iterate(opts ListOptions) *Iterator[api.Credential]
```

### *CredentialsService.List

List calls GET /api/v2/credentials.

```go
func (s *CredentialsService) List(ctx context.Context, opts ListOptions) (*Response[api.CredentialList], error)
```

### *CredentialsService.Patch

Patch calls PATCH /api/v2/credentials/{id}.

```go
func (s *CredentialsService) Patch(ctx context.Context, id string, version string, req api.MergePatch) (*Response[api.Credential], error)
```

### *CredentialsService.Replace

Replace calls PUT /api/v2/credentials/{id}.

```go
func (s *CredentialsService) Replace(ctx context.Context, id string, version string, req api.Credential) (*Response[api.Credential], error)
```

### *Error.Error

```go
func (e *Error) Error() string
```

### *Error.Is

```go
func (e *Error) Is(target error) bool
```

### *IncidentsService.Acknowledge

Acknowledge calls POST /api/v2/incidents/{id}/acknowledge.

```go
func (s *IncidentsService) Acknowledge(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Incident], error)
```

### *IncidentsService.Dismiss

Dismiss calls POST /api/v2/incidents/{id}/dismiss.

```go
func (s *IncidentsService) Dismiss(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Incident], error)
```

### *IncidentsService.Get

Get calls GET /api/v2/incidents/{id}.

```go
func (s *IncidentsService) Get(ctx context.Context, id string) (*Response[api.Incident], error)
```

### *IncidentsService.Iterate

Iterate walks pages lazily.

```go
func (s *IncidentsService) Iterate(opts ListOptions) *Iterator[api.Incident]
```

### *IncidentsService.List

List calls GET /api/v2/incidents.

```go
func (s *IncidentsService) List(ctx context.Context, opts ListOptions) (*Response[api.IncidentList], error)
```

### *IncidentsService.Reopen

Reopen calls POST /api/v2/incidents/{id}/reopen.

```go
func (s *IncidentsService) Reopen(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Incident], error)
```

### *Iterator[T].Err

Err returns the first paging or context error, or nil after normal completion.

```go
func (i *Iterator[T]) Err() error
```

### *Iterator[T].Next

Next advances to the next item, fetching at most one page as needed. It returns
false at the end or after an error; inspect Err to distinguish those cases.

```go
func (i *Iterator[T]) Next(ctx context.Context) bool
```

### *Iterator[T].Value

Value returns the item selected by the most recent successful Next call.

```go
func (i *Iterator[T]) Value() T
```

### *JobTypesService.Create

Build requirement: `externaljobs`.

Create calls POST /api/v2/job-types.

```go
func (s *JobTypesService) Create(ctx context.Context, req api.JobType) (*Response[api.JobType], error)
```

### *JobTypesService.Delete

Build requirement: `externaljobs`.

Delete calls DELETE /api/v2/job-types/{id}.

```go
func (s *JobTypesService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *JobTypesService.Get

Build requirement: `externaljobs`.

Get calls GET /api/v2/job-types/{id}.

```go
func (s *JobTypesService) Get(ctx context.Context, id string) (*Response[api.JobType], error)
```

### *JobTypesService.Iterate

Build requirement: `externaljobs`.

Iterate walks pages lazily.

```go
func (s *JobTypesService) Iterate(opts ListOptions) *Iterator[api.JobType]
```

### *JobTypesService.List

Build requirement: `externaljobs`.

List calls GET /api/v2/job-types.

```go
func (s *JobTypesService) List(ctx context.Context, opts ListOptions) (*Response[api.JobTypeList], error)
```

### *JobTypesService.Replace

Build requirement: `externaljobs`.

Replace calls PUT /api/v2/job-types/{id}.

```go
func (s *JobTypesService) Replace(ctx context.Context, id string, version string, req api.JobType) (*Response[api.JobType], error)
```

### *MonitorsService.Create

Create calls POST /api/v2/monitors.

```go
func (s *MonitorsService) Create(ctx context.Context, req api.Monitor) (*Response[api.Monitor], error)
```

### *MonitorsService.Delete

Delete calls DELETE /api/v2/monitors/{id}.

```go
func (s *MonitorsService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *MonitorsService.Disable

Disable conditionally disables admission without altering other operator controls.

```go
func (s *MonitorsService) Disable(ctx context.Context, id, version string) (*Response[api.Monitor], error)
```

### *MonitorsService.Enable

Enable preserves snoozes, dismissals, and maintenance policy.

```go
func (s *MonitorsService) Enable(ctx context.Context, id, version string) (*Response[api.Monitor], error)
```

### *MonitorsService.Get

Get calls GET /api/v2/monitors/{id}.

```go
func (s *MonitorsService) Get(ctx context.Context, id string) (*Response[api.Monitor], error)
```

### *MonitorsService.Iterate

Iterate walks pages lazily.

```go
func (s *MonitorsService) Iterate(opts ListOptions) *Iterator[api.Monitor]
```

### *MonitorsService.List

List calls GET /api/v2/monitors.

```go
func (s *MonitorsService) List(ctx context.Context, opts ListOptions) (*Response[api.MonitorList], error)
```

### *MonitorsService.Patch

Patch calls PATCH /api/v2/monitors/{id}.

```go
func (s *MonitorsService) Patch(ctx context.Context, id string, version string, req api.MergePatch) (*Response[api.Monitor], error)
```

### *MonitorsService.Recover

Recover calls POST /api/v2/monitors/{id}/recover.

```go
func (s *MonitorsService) Recover(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Operation], error)
```

### *MonitorsService.Replace

Replace calls PUT /api/v2/monitors/{id}.

```go
func (s *MonitorsService) Replace(ctx context.Context, id string, version string, req api.Monitor) (*Response[api.Monitor], error)
```

### *MonitorsService.Snooze

Snooze calls POST /api/v2/monitors/{id}/snooze.

```go
func (s *MonitorsService) Snooze(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Operation], error)
```

### *MonitorsService.Unsnooze

Unsnooze calls POST /api/v2/monitors/{id}/unsnooze.

```go
func (s *MonitorsService) Unsnooze(ctx context.Context, id string, req api.ControlRequest) (*Response[api.Operation], error)
```

### *NotificationEndpointsService.Create

Create calls POST /api/v2/notification-endpoints.

```go
func (s *NotificationEndpointsService) Create(ctx context.Context, req api.NotificationEndpoint) (*Response[api.NotificationEndpoint], error)
```

### *NotificationEndpointsService.Delete

Delete calls DELETE /api/v2/notification-endpoints/{id}.

```go
func (s *NotificationEndpointsService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *NotificationEndpointsService.Get

Get calls GET /api/v2/notification-endpoints/{id}.

```go
func (s *NotificationEndpointsService) Get(ctx context.Context, id string) (*Response[api.NotificationEndpoint], error)
```

### *NotificationEndpointsService.Iterate

Iterate walks pages lazily.

```go
func (s *NotificationEndpointsService) Iterate(opts ListOptions) *Iterator[api.NotificationEndpoint]
```

### *NotificationEndpointsService.List

List calls GET /api/v2/notification-endpoints.

```go
func (s *NotificationEndpointsService) List(ctx context.Context, opts ListOptions) (*Response[api.NotificationEndpointList], error)
```

### *NotificationEndpointsService.Patch

Patch calls PATCH /api/v2/notification-endpoints/{id}.

```go
func (s *NotificationEndpointsService) Patch(ctx context.Context, id string, version string, req api.MergePatch) (*Response[api.NotificationEndpoint], error)
```

### *NotificationEndpointsService.Replace

Replace calls PUT /api/v2/notification-endpoints/{id}.

```go
func (s *NotificationEndpointsService) Replace(ctx context.Context, id string, version string, req api.NotificationEndpoint) (*Response[api.NotificationEndpoint], error)
```

### *NotificationGroupsService.Create

Create calls POST /api/v2/notification-groups.

```go
func (s *NotificationGroupsService) Create(ctx context.Context, req api.NotificationGroup) (*Response[api.NotificationGroup], error)
```

### *NotificationGroupsService.Delete

Delete calls DELETE /api/v2/notification-groups/{id}.

```go
func (s *NotificationGroupsService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *NotificationGroupsService.Get

Get calls GET /api/v2/notification-groups/{id}.

```go
func (s *NotificationGroupsService) Get(ctx context.Context, id string) (*Response[api.NotificationGroup], error)
```

### *NotificationGroupsService.Iterate

Iterate walks pages lazily.

```go
func (s *NotificationGroupsService) Iterate(opts ListOptions) *Iterator[api.NotificationGroup]
```

### *NotificationGroupsService.List

List calls GET /api/v2/notification-groups.

```go
func (s *NotificationGroupsService) List(ctx context.Context, opts ListOptions) (*Response[api.NotificationGroupList], error)
```

### *NotificationGroupsService.Patch

Patch calls PATCH /api/v2/notification-groups/{id}.

```go
func (s *NotificationGroupsService) Patch(ctx context.Context, id string, version string, req api.MergePatch) (*Response[api.NotificationGroup], error)
```

### *NotificationGroupsService.Replace

Replace calls PUT /api/v2/notification-groups/{id}.

```go
func (s *NotificationGroupsService) Replace(ctx context.Context, id string, version string, req api.NotificationGroup) (*Response[api.NotificationGroup], error)
```

### *OperationsService.Activate

Activate calls POST /api/v2/operations/{id}/activate.

```go
func (s *OperationsService) Activate(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.Cancel

Cancel calls POST /api/v2/operations/{id}/cancel.

```go
func (s *OperationsService) Cancel(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.Create

Create calls POST /api/v2/operations with the original preparation ticket.

```go
func (s *OperationsService) Create(ctx context.Context, req api.OperationCreateRequest) (*Response[api.Operation], error)
```

### *OperationsService.CreateReselection

CreateReselection requests a disposable, caller-owned attempt under the
original operation. It does not upload, verify, renew or activate that operation.
Discover server support before use; this candidate contract is additive.

```go
func (s *OperationsService) CreateReselection(ctx context.Context, id string, req api.CollectionReselectionCreateRequest) (*Response[api.CollectionReselectionAttempt], error)
```

### *OperationsService.DiscardReselection

DiscardReselection removes only this owned disposable attempt. It never
cancels the original operation or removes any already committed input row.

```go
func (s *OperationsService) DiscardReselection(ctx context.Context, id, attempt string) (*Response[struct{}], error)
```

### *OperationsService.ExecutionItems

ExecutionItems lazily reads immutable original-input-order results while
pinning the complete summary and collection identity across every page.

```go
func (s *OperationsService) ExecutionItems(id string, opts ExecutionResultPageOptions) *Iterator[api.ApplyResult]
```

### *OperationsService.ExecutionResult

ExecutionResult reads one bounded operation detail page. It preserves pending,
expired and unknown availability as observations; it never activates work.

```go
func (s *OperationsService) ExecutionResult(ctx context.Context, id string, opts ExecutionResultPageOptions) (*Response[api.Operation], error)
```

### *OperationsService.Get

Get calls GET /api/v2/operations/{id}.

```go
func (s *OperationsService) Get(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.GetReselection

GetReselection reads one owned attempt without renewing either expiry. An
attempt disappears after server restart or explicit discard; the original
operation must then be reconciled separately. This method never retries writes.

```go
func (s *OperationsService) GetReselection(ctx context.Context, id, attempt string) (*Response[api.CollectionReselectionAttempt], error)
```

### *OperationsService.Iterate

Iterate walks operation pages lazily without collecting all operations.

```go
func (s *OperationsService) Iterate(opts ListOptions) *Iterator[api.Operation]
```

### *OperationsService.List

List returns one bounded page of collection operations.

```go
func (s *OperationsService) List(ctx context.Context, opts ListOptions) (*Response[api.OperationList], error)
```

### *OperationsService.Preflight

Preflight calls POST /api/v2/collections/preflight.

```go
func (s *OperationsService) Preflight(ctx context.Context, req api.PreflightRequest) (*Response[api.Preflight], error)
```

### *OperationsService.Prepare

Prepare obtains a private, expiring creation ticket without uploading or
activating resources. Retain this ticket with the original frozen collection;
never replace it automatically after attempting Create.

```go
func (s *OperationsService) Prepare(ctx context.Context, req api.CollectionPrepareRequest) (*Response[api.CollectionAdmission], error)
```

### *OperationsService.ResumeReselection

ResumeReselection admits asynchronous upload of the verified missing suffix
to the original operation. It does not validate or activate configuration.
Cancellation stops this HTTP wait, not an already admitted server operation.

```go
func (s *OperationsService) ResumeReselection(ctx context.Context, id, attempt string) (*Response[api.CollectionReselectionAttempt], error)
```

### *OperationsService.Upload

Upload calls PUT /api/v2/operations/{id}/items.

```go
func (s *OperationsService) Upload(ctx context.Context, id string, req api.UploadRequest) (*Response[api.Operation], error)
```

### *OperationsService.UploadReselectionSource

UploadReselectionSource stages one bounded binary part, with no filename,
multipart metadata, JSON wrapper or automatic retry. Only the server can
confirm an identical retry of the last accepted part after a lost reply.

```go
func (s *OperationsService) UploadReselectionSource(ctx context.Context, id, attempt string, part ReselectionSourcePart) (*Response[api.CollectionReselectionAttempt], error)
```

### *OperationsService.Validate

Validate calls POST /api/v2/operations/{id}/validate.

```go
func (s *OperationsService) Validate(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.Validation

Validation reads a sealed result page. Pending compilation is a typed problem
with code validationPending, not a negative validation verdict. The response
has a separate 4 MiB ceiling, further reduced by Config.MaxResponseBytes.

```go
func (s *OperationsService) Validation(ctx context.Context, id string, opts ValidationPageOptions) (*Response[api.ValidationResultPage], error)
```

### *OperationsService.ValidationItems

ValidationItems lazily reads result pages. It pins the complete original
summary and collection identity across pages and retains at most one page.

```go
func (s *OperationsService) ValidationItems(id string, opts ValidationPageOptions) *Iterator[api.ValidationResultItem]
```

### *OperationsService.VerifyReselection

VerifyReselection admits asynchronous proof of all original raw sources and
normalized resources. Poll GetReselection explicitly. A 202 response is not a
verification verdict and this method never starts remainder upload.

```go
func (s *OperationsService) VerifyReselection(ctx context.Context, id, attempt string) (*Response[api.CollectionReselectionAttempt], error)
```

### *OperationsService.Wait

Wait observes operation progress. Cancelling the context never cancels the server operation.

```go
func (s *OperationsService) Wait(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.WaitExecutionResult

WaitExecutionResult waits for an explicitly ready retained result and returns
one first page. Parent cancellation is not result readiness. Canceling ctx
stops this reader only; it never cancels, resubmits or replaces the operation.

```go
func (s *OperationsService) WaitExecutionResult(ctx context.Context, id string) (*Response[api.Operation], error)
```

### *OperationsService.WaitValidation

WaitValidation polls only GET validation, retaining the original handle on
cancellation. It returns one bounded first page and summary, never the entire
collection. It does not submit, cancel, retry or replace a validation request.

```go
func (s *OperationsService) WaitValidation(ctx context.Context, id string) (*Response[api.ValidationResultPage], error)
```

### *PoolsService.Iterate

Iterate walks pages lazily.

```go
func (s *PoolsService) Iterate(opts ListOptions) *Iterator[api.Pool]
```

### *PoolsService.List

List calls GET /api/v2/pools.

```go
func (s *PoolsService) List(ctx context.Context, opts ListOptions) (*Response[api.PoolList], error)
```

### *QueuesService.Iterate

Iterate walks pages lazily.

```go
func (s *QueuesService) Iterate(opts ListOptions) *Iterator[api.Queue]
```

### *QueuesService.List

List calls GET /api/v2/queues.

```go
func (s *QueuesService) List(ctx context.Context, opts ListOptions) (*Response[api.QueueList], error)
```

### *RecipientsService.Create

Create calls POST /api/v2/recipients.

```go
func (s *RecipientsService) Create(ctx context.Context, req api.Recipient) (*Response[api.Recipient], error)
```

### *RecipientsService.Delete

Delete calls DELETE /api/v2/recipients/{id}.

```go
func (s *RecipientsService) Delete(ctx context.Context, id string, version string) (*Response[api.Operation], error)
```

### *RecipientsService.Get

Get calls GET /api/v2/recipients/{id}.

```go
func (s *RecipientsService) Get(ctx context.Context, id string) (*Response[api.Recipient], error)
```

### *RecipientsService.Iterate

Iterate walks pages lazily.

```go
func (s *RecipientsService) Iterate(opts ListOptions) *Iterator[api.Recipient]
```

### *RecipientsService.List

List calls GET /api/v2/recipients.

```go
func (s *RecipientsService) List(ctx context.Context, opts ListOptions) (*Response[api.RecipientList], error)
```

### *RecipientsService.Patch

Patch calls PATCH /api/v2/recipients/{id}.

```go
func (s *RecipientsService) Patch(ctx context.Context, id string, version string, req api.MergePatch) (*Response[api.Recipient], error)
```

### *RecipientsService.Replace

Replace calls PUT /api/v2/recipients/{id}.

```go
func (s *RecipientsService) Replace(ctx context.Context, id string, version string, req api.Recipient) (*Response[api.Recipient], error)
```

### *SLOService.Get

Get calls GET /api/v2/slo.

```go
func (s *SLOService) Get(ctx context.Context) (*Response[api.SLOView], error)
```

### *SystemsService.Iterate

Iterate walks pages lazily.

```go
func (s *SystemsService) Iterate(opts ListOptions) *Iterator[api.System]
```

### *SystemsService.List

List calls GET /api/v2/systems.

```go
func (s *SystemsService) List(ctx context.Context, opts ListOptions) (*Response[api.SystemList], error)
```

### *TransportError.Error

```go
func (e *TransportError) Error() string
```

### *TransportError.Unwrap

```go
func (e *TransportError) Unwrap() error
```

### *WorkerClient.CloseIdleConnections

Build requirement: `externaljobs`.

CloseIdleConnections closes idle transport connections without cancelling
an active handler, request, or server execution.

```go
func (c *WorkerClient) CloseIdleConnections()
```

### *WorkerClient.Heartbeat

Build requirement: `externaljobs`.

Heartbeat reports continued ownership of an execution. Callers must stop
cooperative work when the server response revokes permission.

```go
func (c *WorkerClient) Heartbeat(ctx context.Context, req api.HeartbeatRequest) (*api.HeartbeatResponse, error)
```

### *WorkerClient.LateEvidence

Build requirement: `externaljobs`.

LateEvidence appends evidence without replacing an outcome or operator review.

```go
func (c *WorkerClient) LateEvidence(ctx context.Context, req api.LateEvidenceRequest) (*api.Receipt, error)
```

### *WorkerClient.Poll

Build requirement: `externaljobs`.

Poll requests bounded assignments for available capacity. An assignment is
not permission to invoke a handler; obtain a granted Start disposition first.

```go
func (c *WorkerClient) Poll(ctx context.Context, req api.PollRequest) (*api.Assignments, error)
```

### *WorkerClient.Result

Build requirement: `externaljobs`.

Result reports the same server-owned outcome envelope until a durable receipt.

```go
func (c *WorkerClient) Result(ctx context.Context, req api.Outcome) (*api.Receipt, error)
```

### *WorkerClient.Start

Build requirement: `externaljobs`.

Start authorizes one execution only when Disposition is granted. Repeated
terminal requests never confer permission to execute again.

```go
func (c *WorkerClient) Start(ctx context.Context, req api.StartRequest) (*api.StartResponse, error)
```

### ActionsService

```go
type ActionsService struct {
	// contains filtered or unexported fields
}
```

### AmbiguousError

AmbiguousError means the SDK cannot establish whether a mutation committed.
Inspect the operation handle; never retry a new mutation blindly.

```go
type AmbiguousError struct {
	Cause       error
	OperationID string
}
```

### Client

Client is safe for concurrent use after creation. Treat its services as immutable.

```go
type Client struct {
	Monitors              *MonitorsService
	NotificationEndpoints *NotificationEndpointsService
	NotificationGroups    *NotificationGroupsService
	Recipients            *RecipientsService
	Credentials           *CredentialsService
	Incidents             *IncidentsService
	Actions               *ActionsService
	Operations            *OperationsService
	Queues                *QueuesService
	Pools                 *PoolsService
	Systems               *SystemsService
	SLO                   *SLOService
	// contains filtered or unexported fields
}
```

### Config

Config is copied by New. Callers retain ownership of supplied transports.
HTTPClient redirect policy is always replaced; no authorization follows redirects.

```go
type Config struct {
	BaseURL           string
	AuthToken         string
	TokenSource       TokenSource
	Timeout           time.Duration
	HTTPClient        *http.Client
	RootCAs           *x509.CertPool
	AllowInsecureHTTP bool
	MaxResponseBytes  int64
	// ReadAttempts is opt-in, at most three. Mutations are never automatically retried.
	ReadAttempts int
}
```

### CredentialsService

```go
type CredentialsService struct {
	// contains filtered or unexported fields
}
```

### DefaultMaxResponseBytes

DefaultMaxResponseBytes bounds an ordinary decoded response to 64 MiB.

```go
const DefaultMaxResponseBytes int64 = 64 << 20
```

### ErrConflict ErrNotFound ErrUnavailable ErrFeatureUnavailable ErrExpired ErrUnauthorized ErrInvalid ErrResponseTooLarge ErrAmbiguous ErrNotAdmitted

```go
var (
	ErrConflict           = errors.New("resource version conflict")
	ErrNotFound           = errors.New("resource not found")
	ErrUnavailable        = errors.New("API unavailable")
	ErrFeatureUnavailable = errors.New("feature unavailable")
	ErrExpired            = errors.New("operation or cursor expired")
	ErrUnauthorized       = errors.New("request unauthorized")
	ErrInvalid            = errors.New("invalid request")
	ErrResponseTooLarge   = errors.New("response exceeds configured byte limit")
	ErrAmbiguous          = errors.New("mutation outcome is uncertain")
	// ErrNotAdmitted means a validated response explicitly guarantees that no
	// target resource or action mutation was submitted. A reservation may exist.
	// This does not enable automatic retries and may also match ErrUnavailable.
	ErrNotAdmitted = errors.New("mutation was not submitted")
)
```

### ErrExecutionResultUnsupported ErrExecutionResultExpired ErrExecutionResultPending

```go
var (
	// ErrExecutionResultUnsupported means availability is absent or uses an unknown contract.
	ErrExecutionResultUnsupported = errors.New("execution result availability is unsupported")
	// ErrExecutionResultExpired reports explicit retained-result expiry, not a missing operation.
	ErrExecutionResultExpired = errors.New("execution result has expired")
	// ErrExecutionResultPending means execution or immutable publication is still pending.
	ErrExecutionResultPending = errors.New("execution result is pending")
)
```

### ErrWorkerSessionExpired

Build requirement: `externaljobs`.

ErrWorkerSessionExpired identifies an explicit expired-session response.
A caller may establish a fresh session; it must preserve old execution records.

```go
var ErrWorkerSessionExpired = errors.New("worker session expired")
```

### Error

Error is an RFC 9457 response. Error() intentionally omits server-controlled
detail and field values; inspect Problem explicitly when appropriate.

```go
type Error struct {
	StatusCode             int
	Problem                api.Problem
	RequestID, OperationID string
	// contains filtered or unexported fields
}
```

### ExecutionResultPageOptions

ExecutionResultPageOptions selects a retained page on the existing operation
detail endpoint. Limit defaults to 100 and cannot exceed 500.

```go
type ExecutionResultPageOptions struct {
	Cursor string
	Limit  int
}
```

### IncidentsService

```go
type IncidentsService struct {
	// contains filtered or unexported fields
}
```

### Iterator

Iterator retains one page and fetches the next page only when exhausted.

```go
type Iterator[T any] struct {
	// contains filtered or unexported fields
}
```

### JobTypesService

Build requirement: `externaljobs`.

```go
type JobTypesService struct {
	// contains filtered or unexported fields
}
```

### ListOptions

ListOptions never requests an implicit full-fleet response.

```go
type ListOptions struct {
	Cursor    string
	Limit     int
	Selector  string
	MonitorID string
}
```

### MaxReselectionSourcePartBytes

MaxReselectionSourcePartBytes is the raw binary body ceiling for one source
part. Whole-attempt limits remain authoritative on the server.

```go
const MaxReselectionSourcePartBytes = 1 << 20
```

### MonitorsService

```go
type MonitorsService struct {
	// contains filtered or unexported fields
}
```

### New

New validates connection settings and constructs a client without making a
request. Caller-supplied HTTP clients are copied; their transport remains
caller-owned. Redirects and automatic mutation retries are disabled.

```go
func New(cfg Config) (*Client, error)
```

### NewWorkerClient

Build requirement: `externaljobs`.

NewWorkerClient constructs a protocol client with required authentication.
Construction makes no request and grants no permission to execute work.

```go
func NewWorkerClient(cfg Config) (*WorkerClient, error)
```

### NotificationEndpointsService

```go
type NotificationEndpointsService struct {
	// contains filtered or unexported fields
}
```

### NotificationGroupsService

```go
type NotificationGroupsService struct {
	// contains filtered or unexported fields
}
```

### OperationsService

```go
type OperationsService struct {
	// contains filtered or unexported fields
}
```

### PoolsService

```go
type PoolsService struct {
	// contains filtered or unexported fields
}
```

### QueuesService

```go
type QueuesService struct {
	// contains filtered or unexported fields
}
```

### RecipientsService

```go
type RecipientsService struct {
	// contains filtered or unexported fields
}
```

### ReselectionSourcePart.Format

```go
func (p ReselectionSourcePart) Format(w fmt.State, _ rune)
```

### ReselectionSourcePart.GoString

```go
func (p ReselectionSourcePart) GoString() string
```

### ReselectionSourcePart.MarshalJSON

```go
func (ReselectionSourcePart) MarshalJSON() ([]byte, error)
```

### ReselectionSourcePart.String

```go
func (ReselectionSourcePart) String() string
```

### ReselectionSourcePart

ReselectionSourcePart identifies one exact raw-file part. Data includes comments
and unused source text; send it only to the configured authenticated origin.
Source is one-based, Offset counts raw bytes, and End closes that source.
Callers retain ownership of Data and must not modify it during the request.

```go
type ReselectionSourcePart struct {
	Source int64
	Offset int64
	End    bool
	Data   []byte
}
```

### Response

Response retains identity/progress headers separately from the typed payload.

```go
type Response[T any] struct {
	Data            T
	RequestID       string
	ResourceVersion string
	OperationID     string
	StatusCode      int
	RetryAfter      time.Duration
}
```

### SLOService

```go
type SLOService struct {
	// contains filtered or unexported fields
}
```

### SystemsService

```go
type SystemsService struct {
	// contains filtered or unexported fields
}
```

### TokenSource

TokenSource supplies credentials at request time, supporting explicit rotation.

```go
type TokenSource func(context.Context) (string, error)
```

### TransportError

TransportError preserves errors.Is cancellation while excluding URL/token
details from its printable message.

```go
type TransportError struct{ Cause error }
```

### ValidationPageOptions

ValidationPageOptions selects one original-order retained result page. A zero
Limit requests 100 items; the maximum is 500. Cursors are opaque and scoped to
the original result, authenticated reader, and page limit.

```go
type ValidationPageOptions struct {
	Cursor string
	Limit  int
}
```

### Version

Version identifies the SDK candidate, independently of the server API version.

```go
const Version = "0.1.0-rc.1"
```

### WorkerClient

Build requirement: `externaljobs`.

WorkerClient uses a worker-scoped credential, separate from operator services.
Servers must additionally enforce scoped worker permissions and runtime enablement.

```go
type WorkerClient struct {
	// contains filtered or unexported fields
}
```

## Package api

### APIVersion

APIVersion is the desired-resource wire version, independent of SDK versions.

```go
const APIVersion = "cpra.io/v2"
```

### Accepted Inconclusive Rejected

Defines values for ActionReviewResolution.

```go
const (
	Accepted     ActionReviewResolution = "accepted"
	Inconclusive ActionReviewResolution = "inconclusive"
	Rejected     ActionReviewResolution = "rejected"
)
```

### AccessInfo

AccessInfo The authenticated principal and effective permissions for this request. Notification contacts are separate resources. Missing permissions never grant access.

```go
type AccessInfo struct {
	// Permissions Explicit effective permission identifiers, exactly matching case-sensitive OpenAPI operationId values such as CreateMonitor and AcknowledgeIncident. Clients must not infer write permission solely from a role.
	Permissions []string `json:"permissions"`
	PrincipalID string   `json:"principalId"`

	// Role The named principal role: reader or operator. Future unknown roles do not implicitly grant permissions.
	Role string `json:"role"`
}
```

### Action

Action defines model for Action.

```go
type Action struct {
	// ConflictingEvidence Retained provider observation for the original execution, separate from its held outcome and operator assertion. No provider response body is included.
	ConflictingEvidence *ActionEvidence `json:"conflictingEvidence,omitempty"`
	CreatedAt           *time.Time      `json:"createdAt,omitempty"`
	ExecutionID         string          `json:"executionID,omitempty"`
	ExecutionRevision   string          `json:"executionRevision,omitempty"`

	// ExecutorFenced Whether the original executor is known finished or fenced for review.
	ExecutorFenced bool `json:"executorFenced"`

	// Held An unresolved action hold; independent of the recorded provider outcome.
	Held           bool   `json:"held"`
	ID             string `json:"id"`
	IncarnationUID string `json:"incarnationUID,omitempty"`
	IncidentID     string `json:"incidentID,omitempty"`
	Kind           string `json:"kind,omitempty"`

	// LateEvidence Retained provider observation for the original execution, separate from its held outcome and operator assertion. No provider response body is included.
	LateEvidence *ActionEvidence `json:"lateEvidence,omitempty"`
	MonitorID    string          `json:"monitorID,omitempty"`

	// Outcome Recorded provider outcome classification, unchanged by operator review.
	Outcome   string        `json:"outcome,omitempty"`
	Reason    string        `json:"reason,omitempty"`
	ReceiptID string        `json:"receiptID,omitempty"`
	Review    *ActionReview `json:"review,omitempty"`

	// ReviewRevision Opaque action observation version required for a conditional operator review.
	ReviewRevision string     `json:"reviewRevision,omitempty"`
	State          string     `json:"state"`
	UpdatedAt      *time.Time `json:"updatedAt,omitempty"`
}
```

### ActionEvidence

ActionEvidence Retained provider observation for the original execution, separate from its held outcome and operator assertion. No provider response body is included.

```go
type ActionEvidence struct {
	ExecutionEnd   time.Time `json:"executionEnd"`
	ExecutionStart time.Time `json:"executionStart"`
	Outcome        string    `json:"outcome"`
	RecordedAt     time.Time `json:"recordedAt"`
}
```

### ActionList

ActionList defines model for ActionList.

```go
type ActionList struct {
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []Action  `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### ActionReview

ActionReview defines model for ActionReview.

```go
type ActionReview struct {
	Actor        string   `json:"actor"`
	Conflict     bool     `json:"conflict,omitempty"`
	EvidenceRefs []string `json:"evidenceRefs,omitempty"`

	// Note Optional operator note.
	Note       string                 `json:"note,omitempty"`
	Reason     string                 `json:"reason"`
	Resolution ActionReviewResolution `json:"resolution"`
	ReviewedAt time.Time              `json:"reviewedAt"`
	Revision   string                 `json:"revision"`
}
```

### ActionReviewResolution.Valid

Valid indicates whether the value is a known member of the ActionReviewResolution enum.

```go
func (e ActionReviewResolution) Valid() bool
```

### ActionReviewResolution

ActionReviewResolution defines model for ActionReview.Resolution.

```go
type ActionReviewResolution string
```

### AlertRule

AlertRule defines model for AlertRule.

```go
type AlertRule struct {
	Dispatch     *bool         `json:"dispatch,omitempty"`
	Driver       *DriverConfig `json:"driver,omitempty"`
	EndpointRefs *[]string     `json:"endpointRefs,omitempty"`
	GroupRef     *string       `json:"groupRef,omitempty"`

	// NotifyType Actual notification driver key selected by the Code. Requires recipientRefs or groupRef, and excludes inline driver and direct endpointRefs.
	NotifyType    *string   `json:"notifyType,omitempty"`
	RecipientRefs *[]string `json:"recipientRefs,omitempty"`
}
```

### ApplyItem

ApplyItem defines model for ApplyItem.

```go
type ApplyItem struct {
	// ContentDigest HMAC of ordinal, identity, source coordinates and exact transmitted resource-object bytes.
	ContentDigest string `json:"contentDigest"`

	// ID Exact Kind/ID identity bound by the item MAC.
	ID       string   `json:"id"`
	Ordinal  int64    `json:"ordinal"`
	Resource Resource `json:"resource"`

	// Source Opaque ordered source token; never a filename or URL.
	Source         string `json:"source"`
	SourceDocument int64  `json:"sourceDocument"`
	SourceItem     int64  `json:"sourceItem"`
}
```

### ApplyResult

ApplyResult defines model for ApplyResult.

```go
type ApplyResult struct {
	// Applied Optional retained child observation. Absent while the accepted child is pending or when no child exists; explicit false records a terminal child that was not applied. Null is unsupported.
	Applied *bool `json:"applied,omitempty"`

	// CatalogDecision Original conditional decision: accepted, unchanged, conflict, dependencyBlocked or unattempted. A child failure never changes an accepted decision. Unknown future values remain readable.
	CatalogDecision string `json:"catalogDecision,omitempty"`

	// ChildDisposition Controller disposition of the original accepted catalog child. Never substitutes for catalogDecision and never exists for unchanged or rejected rows.
	ChildDisposition *ExecutionChildDisposition `json:"childDisposition,omitempty"`

	// Committed Optional catalog observation; true means a new mutation was accepted, false means none was accepted. Unchanged is successful without a new mutation. Omission means unavailable; null is unsupported.
	Committed *bool `json:"committed,omitempty"`

	// CommittedIndex Original decision log index; absent for an unattempted input.
	CommittedIndex *int64 `json:"committedIndex,omitempty"`

	// DecidedAt Original decision time; absent for unattempted inputs.
	DecidedAt *time.Time `json:"decidedAt,omitempty"`

	// Generation Generation produced or confirmed by the original decision.
	Generation *int64 `json:"generation,omitempty"`
	ID         string `json:"id"`

	// InputOrdinal One-based original input ordinal, defining result page order.
	InputOrdinal *int64 `json:"inputOrdinal,omitempty"`

	// Kind Resource kind for collection execution rows. Collection id retains Kind/resourceID; ordinary resource receipts retain their existing id form.
	Kind       string `json:"kind,omitempty"`
	Message    string `json:"message,omitempty"`
	NewVersion string `json:"newVersion,omitempty"`
	OldVersion string `json:"oldVersion,omitempty"`

	// OriginalUID Original incarnation from the frozen plan guard.
	OriginalUID string `json:"originalUID,omitempty"`

	// Outcome Compatibility outcome observation. For collection execution, catalogDecision and childDisposition carry separate authoritative facts. Unknown future values remain readable.
	Outcome string `json:"outcome"`

	// PlanOrdinal One-based original execution plan ordinal; independent of input order.
	PlanOrdinal *int64 `json:"planOrdinal,omitempty"`

	// Source Opaque original source token; never a source path, URL, fingerprint or key.
	Source string `json:"source,omitempty"`

	// SourceDocument One-based original source document.
	SourceDocument *int64 `json:"sourceDocument,omitempty"`

	// SourceItem One-based original source item.
	SourceItem *int64 `json:"sourceItem,omitempty"`

	// UID Incarnation produced or confirmed by the original catalog decision.
	UID string `json:"uid,omitempty"`
}
```

### Assignment

Build requirement: `externaljobs`.

Assignment defines model for Assignment.

```go
type Assignment struct {
	CredentialProfile string          `json:"credentialProfile"`
	Deadline          time.Time       `json:"deadline"`
	ExecutionID       string          `json:"executionID"`
	ExecutionRevision string          `json:"executionRevision"`
	IncarnationUID    string          `json:"incarnationUID"`
	JobTypeID         string          `json:"jobTypeID"`
	JobTypeUID        string          `json:"jobTypeUID"`
	JobTypeVersion    string          `json:"jobTypeVersion"`
	Kind              string          `json:"kind"`
	LeaseID           string          `json:"leaseID"`
	MonitorID         string          `json:"monitorID"`
	Parameters        json.RawMessage `json:"parameters"`
	ServerID          string          `json:"serverID"`
	SessionID         string          `json:"sessionID"`
	WorkerUID         string          `json:"workerUID"`
}
```

### Assignments

Build requirement: `externaljobs`.

Assignments defines model for Assignments.

```go
type Assignments struct {
	ClientSessionID  string       `json:"clientSessionID"`
	Items            []Assignment `json:"items"`
	PollSequence     int64        `json:"pollSequence"`
	ServerID         string       `json:"serverID"`
	SessionExpiresAt time.Time    `json:"sessionExpiresAt"`
	SessionID        string       `json:"sessionID"`
	WorkerUID        string       `json:"workerUID"`
}
```

### AuthorizationChanged Expired InputMismatch OperationChanged QuotaExceeded StorageUnavailable TransferFailed VerificationFailed

Defines values for CollectionReselectionAttemptErrorCode.

```go
const (
	AuthorizationChanged CollectionReselectionAttemptErrorCode = "authorization_changed"
	Expired              CollectionReselectionAttemptErrorCode = "expired"
	InputMismatch        CollectionReselectionAttemptErrorCode = "input_mismatch"
	OperationChanged     CollectionReselectionAttemptErrorCode = "operation_changed"
	QuotaExceeded        CollectionReselectionAttemptErrorCode = "quota_exceeded"
	StorageUnavailable   CollectionReselectionAttemptErrorCode = "storage_unavailable"
	TransferFailed       CollectionReselectionAttemptErrorCode = "transfer_failed"
	VerificationFailed   CollectionReselectionAttemptErrorCode = "verification_failed"
)
```

### Capabilities

Capabilities defines model for Capabilities.

```go
type Capabilities struct {
	APIVersions []string            `json:"apiVersions"`
	Drivers     map[string][]string `json:"drivers"`
	PatchTypes  []string            `json:"patchTypes"`

	// ResourceOperations Supported operations by resource; availability does not grant authorization.
	ResourceOperations map[string][]string `json:"resourceOperations,omitempty"`
	Resources          []string            `json:"resources"`
}
```

### CheckSpec

CheckSpec defines model for CheckSpec.

```go
type CheckSpec struct {
	Driver           DriverConfig `json:"driver"`
	Groups           *[]string    `json:"groups,omitempty"`
	HealthyThreshold *int64       `json:"healthyThreshold,omitempty"`

	// Interval Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Interval    Duration `json:"interval"`
	MaxFailures *int64   `json:"maxFailures,omitempty"`
	Retries     *int64   `json:"retries,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout            Duration `json:"timeout"`
	UnhealthyThreshold *int64   `json:"unhealthyThreshold,omitempty"`
}
```

### CodeNotificationDatadog

CodeNotificationDatadog defines model for CodeNotificationDatadog.

```go
type CodeNotificationDatadog struct {
	APIKey *string   `json:"apiKey,omitempty"`
	AppKey *string   `json:"appKey,omitempty"`
	Site   *string   `json:"site,omitempty"`
	Tags   *[]string `json:"tags,omitempty"`
	URL    *string   `json:"url,omitempty"`
}
```

### CodeNotificationDiscord

CodeNotificationDiscord defines model for CodeNotificationDiscord.

```go
type CodeNotificationDiscord struct {
	WebhookURL *string `json:"webhookUrl,omitempty"`
}
```

### CodeNotificationEmail

CodeNotificationEmail defines model for CodeNotificationEmail.

```go
type CodeNotificationEmail struct {
	AllowInsecure *bool   `json:"allowInsecure,omitempty"`
	From          *string `json:"from,omitempty"`
	Server        *string `json:"server,omitempty"`
	Subject       *string `json:"subject,omitempty"`
	To            *string `json:"to,omitempty"`
}
```

### CodeNotificationLog

CodeNotificationLog defines model for CodeNotificationLog.

```go
type CodeNotificationLog struct {
	File *string `json:"file,omitempty"`
}
```

### CodeNotificationMattermost

CodeNotificationMattermost defines model for CodeNotificationMattermost.

```go
type CodeNotificationMattermost struct {
	Channel    *string `json:"channel,omitempty"`
	Username   *string `json:"username,omitempty"`
	WebhookURL *string `json:"webhookUrl,omitempty"`
}
```

### CodeNotificationOpsgenie

CodeNotificationOpsgenie defines model for CodeNotificationOpsgenie.

```go
type CodeNotificationOpsgenie struct {
	APIKey *string `json:"apiKey,omitempty"`
	URL    *string `json:"url,omitempty"`
}
```

### CodeNotificationPagerDuty

CodeNotificationPagerDuty defines model for CodeNotificationPagerDuty.

```go
type CodeNotificationPagerDuty struct {
	RoutingKey *string `json:"routingKey,omitempty"`
	URL        *string `json:"url,omitempty"`
}
```

### CodeNotificationPushover

CodeNotificationPushover defines model for CodeNotificationPushover.

```go
type CodeNotificationPushover struct {
	AppToken *string `json:"appToken,omitempty"`
	Expire   *int64  `json:"expire,omitempty"`
	Priority *int64  `json:"priority,omitempty"`
	Retry    *int64  `json:"retry,omitempty"`
	Sound    *string `json:"sound,omitempty"`
	Title    *string `json:"title,omitempty"`
	URL      *string `json:"url,omitempty"`
	UserKey  *string `json:"userKey,omitempty"`
}
```

### CodeNotificationSlack

CodeNotificationSlack defines model for CodeNotificationSlack.

```go
type CodeNotificationSlack struct {
	WebHook *string `json:"hook,omitempty"`
}
```

### CodeNotificationTeams

CodeNotificationTeams defines model for CodeNotificationTeams.

```go
type CodeNotificationTeams struct {
	WebhookURL *string `json:"webhookUrl,omitempty"`
}
```

### CodeNotificationTelegram

CodeNotificationTelegram defines model for CodeNotificationTelegram.

```go
type CodeNotificationTelegram struct {
	BotToken *string `json:"botToken,omitempty"`
	ChatID   *string `json:"chatId,omitempty"`
	TestMode *bool   `json:"testMode,omitempty"`
	URL      *string `json:"url,omitempty"`
}
```

### CodeNotificationTwilio

CodeNotificationTwilio defines model for CodeNotificationTwilio.

```go
type CodeNotificationTwilio struct {
	AccountSID *string `json:"accountSid,omitempty"`
	AuthToken  *string `json:"authToken,omitempty"`
	From       *string `json:"from,omitempty"`
	To         *string `json:"to,omitempty"`
	URL        *string `json:"url,omitempty"`
}
```

### CodeNotificationVictorOps

CodeNotificationVictorOps defines model for CodeNotificationVictorOps.

```go
type CodeNotificationVictorOps struct {
	EntityID        *string `json:"entityId,omitempty"`
	MessageType     *string `json:"messageType,omitempty"`
	RESTEndpointKey *string `json:"restEndpointKey,omitempty"`
	RoutingKey      *string `json:"routingKey,omitempty"`
	URL             *string `json:"url,omitempty"`
}
```

### CodeNotificationWebhook

CodeNotificationWebhook defines model for CodeNotificationWebhook.

```go
type CodeNotificationWebhook struct {
	Headers *map[string]string `json:"headers,omitempty"`
	Method  *string            `json:"method,omitempty"`
	URL     *string            `json:"url,omitempty"`
}
```

### CollectionAdmission.Format

```go
func (r CollectionAdmission) Format(w fmt.State, _ rune)
```

### CollectionAdmission.GoString

```go
func (r CollectionAdmission) GoString() string
```

### CollectionAdmission.String

String omits the opaque creation ticket, including under nested formatting.

```go
func (CollectionAdmission) String() string
```

### CollectionAdmission

CollectionAdmission defines model for CollectionAdmission.

```go
type CollectionAdmission struct {
	ExpiresAt time.Time `json:"expiresAt"`

	// Ticket Private opaque creation ticket; keep in client memory with the original frozen input and omit from diagnostics.
	Ticket string `json:"ticket"`
}
```

### CollectionPrepareRequest.Format

```go
func (r CollectionPrepareRequest) Format(w fmt.State, _ rune)
```

### CollectionPrepareRequest.GoString

```go
func (r CollectionPrepareRequest) GoString() string
```

### CollectionPrepareRequest.String

String omits the private key and source commitment used to prepare a ticket.

```go
func (CollectionPrepareRequest) String() string
```

### CollectionPrepareRequest

CollectionPrepareRequest Prepare an authenticated creation ticket for one frozen collection before its first Create request. Preparing a ticket never uploads or activates resources.

```go
type CollectionPrepareRequest struct {
	// ContentDigest Opaque keyed inventory commitment; requires the original frozen key and bytes to resume.
	ContentDigest string `json:"contentDigest"`

	// IdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.
	IdentityFormat CollectionPrepareRequestIdentityFormat `json:"identityFormat"`

	// IdentityKey Private per-frozen-collection HMAC key. Send only to the configured authenticated server. Never return, log or persist unencrypted.
	IdentityKey *string `json:"identityKey,omitempty"`
	ItemCount   int64   `json:"itemCount"`

	// NormalizationProfile Optional versioned raw-file normalization contract, bound to the original admission and encrypted collection identity. Omit for unprofiled or typed-resource inputs. This field alone does not enable source reselection.
	NormalizationProfile string `json:"normalizationProfile,omitempty"`

	// SourceFingerprint Private keyed source commitment. Source paths and URLs remain client-local.
	SourceFingerprint *string `json:"sourceFingerprint,omitempty"`
}
```

### CollectionPrepareRequestIdentityFormat.Valid

Valid indicates whether the value is a known member of the CollectionPrepareRequestIdentityFormat enum.

```go
func (e CollectionPrepareRequestIdentityFormat) Valid() bool
```

### CollectionPrepareRequestIdentityFormat

CollectionPrepareRequestIdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.

```go
type CollectionPrepareRequestIdentityFormat string
```

### CollectionPrepareRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1

Defines values for CollectionPrepareRequestIdentityFormat.

```go
const (
	CollectionPrepareRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1 CollectionPrepareRequestIdentityFormat = "cpra.collection.hmac-sha256-json-bytes.v1"
)
```

### CollectionReselectionAttempt

CollectionReselectionAttempt Bounded observation of an owned, disposable source-reselection attempt. Source bytes include comments and unused text. This response contains no file names, source paths, item digests, source fingerprints, inventory keys or resource plaintext. Verification and remainder upload do not activate configuration.

```go
type CollectionReselectionAttempt struct {
	// ErrorCode Safe failure category; contains no input text, path, provider diagnostic or private commitment.
	ErrorCode *CollectionReselectionAttemptErrorCode `json:"errorCode,omitempty"`

	// ExpiresAt Fixed attempt expiry. Reads and failed requests do not renew this deadline or the original operation.
	ExpiresAt time.Time `json:"expiresAt"`

	// ID Opaque attempt identity. This is not authorization.
	ID string `json:"id"`

	// NextOffset Next raw byte offset within nextSource; zero after all sources are complete.
	NextOffset int64 `json:"nextOffset"`

	// NextSource Next sequential source ordinal while input is incomplete; zero after all sources are complete.
	NextSource int64 `json:"nextSource"`

	// NormalizationProfile The original supported file normalization profile; no parser source, key or commitment is returned.
	NormalizationProfile string `json:"normalizationProfile"`

	// OperationID The unchanged original collection operation handle.
	OperationID string `json:"operationID"`

	// OperationUploaded Observed number of uploaded resources in the original operation; not an activation or execution count.
	OperationUploaded int64                             `json:"operationUploaded"`
	Phase             CollectionReselectionAttemptPhase `json:"phase"`
	RawBytes          int64                             `json:"rawBytes"`
	SourceCount       int64                             `json:"sourceCount"`
	SourcesCompleted  int64                             `json:"sourcesCompleted"`
}
```

### CollectionReselectionAttemptErrorCode.Valid

Valid indicates whether the value is a known member of the CollectionReselectionAttemptErrorCode enum.

```go
func (e CollectionReselectionAttemptErrorCode) Valid() bool
```

### CollectionReselectionAttemptErrorCode

CollectionReselectionAttemptErrorCode Safe failure category; contains no input text, path, provider diagnostic or private commitment.

```go
type CollectionReselectionAttemptErrorCode string
```

### CollectionReselectionAttemptPhase.Valid

Valid indicates whether the value is a known member of the CollectionReselectionAttemptPhase enum.

```go
func (e CollectionReselectionAttemptPhase) Valid() bool
```

### CollectionReselectionAttemptPhase

CollectionReselectionAttemptPhase defines model for CollectionReselectionAttempt.Phase.

```go
type CollectionReselectionAttemptPhase string
```

### CollectionReselectionCreateRequest

CollectionReselectionCreateRequest Create a disposable attempt owned by the authenticated original operation owner. The explicit base file normalization profile must match the original operation. This request neither extends the original operation expiry nor creates a replacement operation. Attempts are process-local and unavailable after restart.

```go
type CollectionReselectionCreateRequest struct {
	NormalizationProfile string `json:"normalizationProfile"`
	SourceCount          int64  `json:"sourceCount"`
}
```

### Completed Failed Transferring Uploading Verified Verifying

Defines values for CollectionReselectionAttemptPhase.

```go
const (
	Completed    CollectionReselectionAttemptPhase = "completed"
	Failed       CollectionReselectionAttemptPhase = "failed"
	Transferring CollectionReselectionAttemptPhase = "transferring"
	Uploading    CollectionReselectionAttemptPhase = "uploading"
	Verified     CollectionReselectionAttemptPhase = "verified"
	Verifying    CollectionReselectionAttemptPhase = "verifying"
)
```

### ControlRequest

ControlRequest defines model for ControlRequest.

```go
type ControlRequest struct {
	// Duration Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Duration     Duration `json:"duration,omitempty"`
	EvidenceRefs []string `json:"evidenceRefs,omitempty"`
	IncidentID   string   `json:"incidentID,omitempty"`
	Note         string   `json:"note,omitempty"`
	Reason       string   `json:"reason,omitempty"`
	Resolution   string   `json:"resolution,omitempty"`
	Revision     string   `json:"revision"`
}
```

### Credential

Credential defines model for Credential.

```go
type Credential struct {
	APIVersion string          `json:"apiVersion"`
	Kind       string          `json:"kind"`
	Metadata   Metadata        `json:"metadata"`
	Spec       CredentialSpec  `json:"spec"`
	Status     json.RawMessage `json:"status,omitempty"`
}
```

### CredentialList

CredentialList defines model for CredentialList.

```go
type CredentialList struct {
	GeneratedAt time.Time    `json:"generatedAt,omitempty"`
	Items       []Credential `json:"items"`
	NextCursor  string       `json:"nextCursor,omitempty"`
	Snapshot    string       `json:"snapshot,omitempty"`
}
```

### CredentialSpec

CredentialSpec defines model for CredentialSpec.

```go
type CredentialSpec struct {
	Description *string `json:"description,omitempty"`
	Value       *string `json:"value,omitempty"`
}
```

### DecodeResource

DecodeResource validates a source-neutral resource envelope.

```go
func DecodeResource(raw []byte) (Resource, error)
```

### DecodeResponse

DecodeResponse permits additive fields from newer servers while checking the
presence of required contract fields. Unavailable zero values must be explicit.
Unlike mutation decoding, forward-compatible response fields are not rejected.

```go
func DecodeResponse(raw []byte, out any) error
```

### Driver

Driver constructs a concrete driver envelope while preserving optional fields.

```go
func Driver(category, kind string, config any) (DriverConfig, error)
```

### DriverConfig

DriverConfig defines model for DriverConfig.

```go
type DriverConfig struct {
	Config         json.RawMessage    `json:"config"`
	CredentialRefs *map[string]string `json:"credentialRefs,omitempty"`
	Type           string             `json:"type"`
}
```

### Duration

Duration Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.

```go
type Duration = string
```

### ErrUnsupportedDriver

ErrUnsupportedDriver reports a driver absent from this SDK's selected schema.

```go
var ErrUnsupportedDriver = errors.New("driver is unsupported by this SDK build")
```

### Event

Event defines model for Event.

```go
type Event struct {
	ActionID string `json:"actionID,omitempty"`

	// ActionKind Original action category, when the event belongs to an action.
	ActionKind string `json:"actionKind,omitempty"`
	Actor      string `json:"actor,omitempty"`

	// Color Original notification Code, when applicable.
	Color string `json:"color,omitempty"`

	// ControlRevision Original control or incident attention revision, when applicable.
	ControlRevision string `json:"controlRevision,omitempty"`

	// Endpoint Original notification endpoint index; explicit zero is retained when applicable.
	Endpoint *int `json:"endpoint,omitempty"`

	// EvidenceRefs Opaque operator-supplied evidence references. CPRa does not fetch these references.
	EvidenceRefs []string `json:"evidenceRefs,omitempty"`

	// ExecutionRevision Original execution revision, never a configuration or control version.
	ExecutionRevision string `json:"executionRevision,omitempty"`
	ID                string `json:"id"`
	IncidentID        string `json:"incidentID,omitempty"`
	Kind              string `json:"kind"`
	MonitorID         string `json:"monitorID"`

	// Note Operator note recorded with the original audited event; never provider credentials.
	Note string `json:"note,omitempty"`

	// Outcome Recorded safe outcome classification; never a provider response body.
	Outcome string    `json:"outcome,omitempty"`
	Reason  string    `json:"reason,omitempty"`
	Time    time.Time `json:"time"`
}
```

### EventList

EventList defines model for EventList.

```go
type EventList struct {
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []Event   `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### ExecutionChildDisposition

ExecutionChildDisposition Controller disposition of the original accepted catalog child. Never substitutes for catalogDecision and never exists for unchanged or rejected rows.

```go
type ExecutionChildDisposition struct {
	// InvalidatedByRestore Original explicit restore marker, distinct from ordinary supersession.
	InvalidatedByRestore string `json:"invalidatedByRestore,omitempty"`

	// OperationID Original accepted child operation handle.
	OperationID string `json:"operationID"`

	// Outcome applied, projection_failed or superseded; absent while pending.
	Outcome string `json:"outcome,omitempty"`

	// State pending, completed, failed or partial. Unknown future values are observational only.
	State string `json:"state"`

	// UpdatedAt Original retained terminal observation; absent while pending.
	UpdatedAt *time.Time `json:"updatedAt,omitempty"`
}
```

### ExecutionResultAvailability

ExecutionResultAvailability Separate result availability, independent of Operation.state. Omission means the server has not reported execution-result support for this operation. Errors remain typed problems.

```go
type ExecutionResultAvailability struct {
	// Counts Scalar execution observations. Catalog acceptance and controller application are independent; explicit zero is available, omission of the entire object is unavailable.
	Counts *ExecutionResultCounts `json:"counts,omitempty"`

	// State pending: original execution or publication has not sealed; ready: immutable pages are available; expired: retained result deadline passed. Unknown values remain read-only. A canceled parent may still be pending.
	State string `json:"state"`

	// Summary Immutable original identities, terminal counters and sealed result descriptor. Presence alone never establishes availability; consult executionResult.state.
	Summary *ExecutionResultSummary `json:"summary,omitempty"`
}
```

### ExecutionResultCounts

ExecutionResultCounts Scalar execution observations. Catalog acceptance and controller application are independent; explicit zero is available, omission of the entire object is unavailable.

```go
type ExecutionResultCounts struct {
	// Accepted New catalog mutations accepted. Controller disposition does not change this count.
	Accepted int64 `json:"accepted"`

	// ChildApplied Accepted children with completed/applied evidence.
	ChildApplied int64 `json:"childApplied"`

	// ChildFailed Accepted children with failed/projection_failed evidence.
	ChildFailed int64 `json:"childFailed"`

	// ChildInvalidated Accepted children invalidated by explicit restore, separately counted.
	ChildInvalidated int64 `json:"childInvalidated"`

	// ChildPending Accepted children without a retained terminal disposition.
	ChildPending int64 `json:"childPending"`

	// ChildSuperseded Accepted children superseded independently of explicit restore.
	ChildSuperseded int64 `json:"childSuperseded"`

	// Conflicts Original conditional decisions rejected by a conflicting version or incarnation.
	Conflicts int64 `json:"conflicts"`

	// DependencyBlocked Original decisions blocked by their frozen dependencies.
	DependencyBlocked int64 `json:"dependencyBlocked"`

	// Processed Original plan decisions recorded; excludes the unattempted suffix.
	Processed int64 `json:"processed"`

	// Unattempted Original inputs never decided, certified by the final stop observation; zero while applying.
	Unattempted int64 `json:"unattempted"`

	// Unchanged Successful unchanged decisions with no new mutation or controller child.
	Unchanged int64 `json:"unchanged"`
}
```

### ExecutionResultSummary

ExecutionResultSummary Immutable original identities, terminal counters and sealed result descriptor. Presence alone never establishes availability; consult executionResult.state.

```go
type ExecutionResultSummary struct {
	// Accepted New catalog mutations accepted. Controller disposition does not change this count.
	Accepted int64 `json:"accepted"`

	// Bytes Canonical sealed item bytes.
	Bytes int64 `json:"bytes"`

	// ChildApplied Accepted children with completed/applied evidence.
	ChildApplied int64 `json:"childApplied"`

	// ChildFailed Accepted children with failed/projection_failed evidence.
	ChildFailed int64 `json:"childFailed"`

	// ChildInvalidated Accepted children invalidated by explicit restore, separately counted.
	ChildInvalidated int64 `json:"childInvalidated"`

	// ChildPending Accepted children without a retained terminal disposition.
	ChildPending int64 `json:"childPending"`

	// ChildSuperseded Accepted children superseded independently of explicit restore.
	ChildSuperseded int64 `json:"childSuperseded"`

	// Conflicts Original conditional decisions rejected by a conflicting version or incarnation.
	Conflicts int64 `json:"conflicts"`

	// DependencyBlocked Original decisions blocked by their frozen dependencies.
	DependencyBlocked int64 `json:"dependencyBlocked"`

	// Digest Canonical sealed item digest.
	Digest string `json:"digest"`

	// ExpiresAt Retained result deadline, independent of cancellation receipt expiry.
	ExpiresAt time.Time `json:"expiresAt"`

	// FinalizedAt Immutable finalization time; begins the retention cohort.
	FinalizedAt time.Time `json:"finalizedAt"`

	// ItemCount Original input count; public pages follow inputOrdinal, independently of planOrdinal.
	ItemCount int64 `json:"itemCount"`

	// Outcome Final original parent outcome: completed, partial, failed, canceled or invalidated. Unknown future values are read-only observations.
	Outcome string `json:"outcome"`

	// PlanDigest Original immutable plan digest.
	PlanDigest string `json:"planDigest"`

	// PlanID Original immutable plan identity.
	PlanID string `json:"planID"`

	// Processed Original plan decisions recorded; excludes the unattempted suffix.
	Processed int64 `json:"processed"`

	// ResultID Original activation identity; never regenerated for result publication.
	ResultID string `json:"resultID"`

	// Unattempted Original inputs never decided, certified by the final stop observation; zero while applying.
	Unattempted int64 `json:"unattempted"`

	// Unchanged Successful unchanged decisions with no new mutation or controller child.
	Unchanged int64 `json:"unchanged"`

	// UploadID Original upload identity.
	UploadID string `json:"uploadID"`
}
```

### Explanation

Explanation defines model for Explanation.

```go
type Explanation struct {
	Description string          `json:"description"`
	Resource    string          `json:"resource"`
	Schema      json.RawMessage `json:"schema"`
}
```

### ExternalConfig

Build requirement: `externaljobs`.

ExternalConfig defines model for ExternalConfig.

```go
type ExternalConfig struct {
	CredentialProfile string          `json:"credentialProfile,omitempty"`
	JobTypeID         string          `json:"jobTypeID"`
	Parameters        json.RawMessage `json:"parameters"`
	Version           string          `json:"version"`
}
```

### FieldError

FieldError defines model for FieldError.

```go
type FieldError struct {
	Field   string `json:"field"`
	Message string `json:"message"`
	Reason  string `json:"reason,omitempty"`
}
```

### Health

Health defines model for Health.

```go
type Health struct {
	Available   bool      `json:"available"`
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Reason      string    `json:"reason,omitempty"`
}
```

### HeartbeatRequest

Build requirement: `externaljobs`.

HeartbeatRequest defines model for HeartbeatRequest.

```go
type HeartbeatRequest struct {
	ExecutionID string `json:"executionID"`
	GrantID     string `json:"grantID"`
	ServerID    string `json:"serverID"`
	SessionID   string `json:"sessionID"`
	WorkerID    string `json:"workerID"`
}
```

### HeartbeatResponse

Build requirement: `externaljobs`.

HeartbeatResponse defines model for HeartbeatResponse.

```go
type HeartbeatResponse struct {
	Accepted    bool   `json:"accepted"`
	ExecutionID string `json:"executionID"`
	GrantID     string `json:"grantID"`
	ServerID    string `json:"serverID"`
	SessionID   string `json:"sessionID"`
	WorkerUID   string `json:"workerUID"`
}
```

### Incident

Incident defines model for Incident.

```go
type Incident struct {
	AcknowledgedAt time.Time `json:"acknowledgedAt,omitempty"`
	AcknowledgedBy string    `json:"acknowledgedBy,omitempty"`
	ClosedAt       time.Time `json:"closedAt,omitempty"`
	Dismissed      bool      `json:"dismissed,omitempty"`
	ID             string    `json:"id"`
	MonitorID      string    `json:"monitorID"`
	OpenedAt       time.Time `json:"openedAt,omitempty"`
	Revision       string    `json:"revision"`
	State          string    `json:"state"`
}
```

### IncidentList

IncidentList defines model for IncidentList.

```go
type IncidentList struct {
	GeneratedAt time.Time  `json:"generatedAt,omitempty"`
	Items       []Incident `json:"items"`
	NextCursor  string     `json:"nextCursor,omitempty"`
	Snapshot    string     `json:"snapshot,omitempty"`
}
```

### InterventionTargetAWS

InterventionTargetAWS defines model for InterventionTargetAWS.

```go
type InterventionTargetAWS struct {
	InstanceID *string `json:"instanceId,omitempty"`
	Operation  *string `json:"operation,omitempty"`
	Region     *string `json:"region,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout *Duration `json:"timeout,omitempty"`
	Type    *string   `json:"type,omitempty"`
}
```

### InterventionTargetDocker

InterventionTargetDocker defines model for InterventionTargetDocker.

```go
type InterventionTargetDocker struct {
	Container *string `json:"container,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout *Duration `json:"timeout,omitempty"`
	Type    *string   `json:"type,omitempty"`
}
```

### InterventionTargetKubernetes

InterventionTargetKubernetes defines model for InterventionTargetKubernetes.

```go
type InterventionTargetKubernetes struct {
	Kind           *string `json:"kind,omitempty"`
	KubeconfigPath *string `json:"kubeconfigPath,omitempty"`
	Name           *string `json:"name,omitempty"`
	Namespace      *string `json:"namespace,omitempty"`
	Replicas       *int64  `json:"replicas,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout *Duration `json:"timeout,omitempty"`
	Type    *string   `json:"type,omitempty"`
}
```

### InterventionTargetSystemd

InterventionTargetSystemd defines model for InterventionTargetSystemd.

```go
type InterventionTargetSystemd struct {
	Mode *string `json:"mode,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout *Duration `json:"timeout,omitempty"`
	Type    *string   `json:"type,omitempty"`
	Unit    *string   `json:"unit,omitempty"`
}
```

### InterventionTargetWebhook

InterventionTargetWebhook defines model for InterventionTargetWebhook.

```go
type InterventionTargetWebhook struct {
	Body    *string            `json:"body,omitempty"`
	Headers *map[string]string `json:"headers,omitempty"`
	Method  *string            `json:"method,omitempty"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout *Duration `json:"timeout,omitempty"`
	Type    *string   `json:"type,omitempty"`
	URL     *string   `json:"url,omitempty"`
}
```

### JobType

Build requirement: `externaljobs`.

JobType defines model for JobType.

```go
type JobType struct {
	APIVersion string      `json:"apiVersion"`
	Kind       string      `json:"kind"`
	Metadata   Metadata    `json:"metadata"`
	Spec       JobTypeSpec `json:"spec"`
}
```

### JobTypeList

Build requirement: `externaljobs`.

JobTypeList defines model for JobTypeList.

```go
type JobTypeList struct {
	Items      []JobType `json:"items"`
	NextCursor string    `json:"nextCursor,omitempty"`
}
```

### JobTypeSpec

Build requirement: `externaljobs`.

JobTypeSpec defines model for JobTypeSpec.

```go
type JobTypeSpec struct {
	Handler         string          `json:"handler"`
	Kind            string          `json:"kind"`
	ParameterSchema json.RawMessage `json:"parameterSchema"`
	ProtocolVersion string          `json:"protocolVersion"`
	RejectionCodes  []string        `json:"rejectionCodes,omitempty"`
	ResultSchema    json.RawMessage `json:"resultSchema"`

	// Timeout Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Timeout Duration `json:"timeout,omitempty"`
	Version string   `json:"version"`
}
```

### LateEvidenceRequest

Build requirement: `externaljobs`.

LateEvidenceRequest defines model for LateEvidenceRequest.

```go
type LateEvidenceRequest struct {
	Data              json.RawMessage `json:"data"`
	Diagnostic        string          `json:"diagnostic"`
	Evidence          []string        `json:"evidence"`
	EvidenceID        string          `json:"evidenceID"`
	ExecutionID       string          `json:"executionID"`
	OriginalReceiptID string          `json:"originalReceiptID"`
	ServerID          string          `json:"serverID"`
	WorkerUID         string          `json:"workerUID"`
}
```

### MaintenanceWindow

MaintenanceWindow defines model for MaintenanceWindow.

```go
type MaintenanceWindow struct {
	Cron *string `json:"cron,omitempty"`

	// Duration Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Duration *Duration  `json:"duration,omitempty"`
	End      *time.Time `json:"end,omitempty"`
	Start    *time.Time `json:"start,omitempty"`
	Timezone *string    `json:"timezone,omitempty"`
}
```

### MaxResourceBytes

MaxResourceBytes bounds each encoded desired resource to 1 MiB.

```go
const MaxResourceBytes = 1 << 20
```

### Measurement

Measurement defines model for Measurement.

```go
type Measurement struct {
	Available bool   `json:"available"`
	Reason    string `json:"reason,omitempty"`

	// Value Meaningful only when available is true; units are defined by the containing field.
	Value float64 `json:"value"`
}
```

### MergePatch

MergePatch defines model for MergePatch.

```go
type MergePatch = json.RawMessage
```

### Metadata

Metadata defines model for Metadata.

```go
type Metadata struct {
	Generation      int64              `json:"generation,omitempty"`
	ID              string             `json:"id"`
	Labels          *map[string]string `json:"labels,omitempty"`
	Name            *string            `json:"name,omitempty"`
	ResourceVersion string             `json:"resourceVersion,omitempty"`
	UID             string             `json:"uid,omitempty"`
}
```

### Metrics

Metrics defines model for Metrics.

```go
type Metrics struct {
	GeneratedAt      time.Time `json:"generatedAt"`
	Pools            []Pool    `json:"pools,omitempty"`
	PoolsAvailable   bool      `json:"poolsAvailable"`
	Queues           []Queue   `json:"queues,omitempty"`
	QueuesAvailable  bool      `json:"queuesAvailable"`
	SLO              SLOView   `json:"slo,omitempty"`
	Systems          []System  `json:"systems,omitempty"`
	SystemsAvailable bool      `json:"systemsAvailable"`
}
```

### Monitor

Monitor defines model for Monitor.

```go
type Monitor struct {
	APIVersion string        `json:"apiVersion"`
	Kind       string        `json:"kind"`
	Metadata   Metadata      `json:"metadata"`
	Spec       MonitorSpec   `json:"spec"`
	Status     MonitorStatus `json:"status,omitempty"`
}
```

### MonitorList

MonitorList defines model for MonitorList.

```go
type MonitorList struct {
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []Monitor `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### MonitorSpec

MonitorSpec defines model for MonitorSpec.

```go
type MonitorSpec struct {
	Check                 CheckSpec             `json:"check"`
	Enabled               *bool                 `json:"enabled,omitempty"`
	Maintenance           *[]MaintenanceWindow  `json:"maintenance,omitempty"`
	NotificationGroupRefs *[]string             `json:"notificationGroupRefs,omitempty"`
	Notifications         *map[string]AlertRule `json:"notifications,omitempty"`
	Recovery              *RecoverySpec         `json:"recovery,omitempty"`
	Tags                  *[]string             `json:"tags,omitempty"`
}
```

### MonitorStatus

MonitorStatus defines model for MonitorStatus.

```go
type MonitorStatus struct {
	ControlRevision    string      `json:"controlRevision,omitempty"`
	ExecutionRevision  string      `json:"executionRevision,omitempty"`
	Health             string      `json:"health,omitempty"`
	IncidentID         string      `json:"incidentID,omitempty"`
	LastCheckLatencyMS Measurement `json:"lastCheckLatencyMs,omitempty"`
	LastCheckedAt      time.Time   `json:"lastCheckedAt,omitempty"`
	ObservedGeneration int64       `json:"observedGeneration,omitempty"`
	SnoozedUntil       time.Time   `json:"snoozedUntil,omitempty"`
	StatusRevision     string      `json:"statusRevision,omitempty"`
	UnknownActions     int64       `json:"unknownActions,omitempty"`
}
```

### NormalizeManifestDriver

NormalizeManifestDriver maps manifest driver fields to their API names.
User-defined map keys, such as HTTP headers, retain their original spelling.

```go
func NormalizeManifestDriver(category, kind string, raw json.RawMessage) (json.RawMessage, error)
```

### NotificationEndpoint

NotificationEndpoint defines model for NotificationEndpoint.

```go
type NotificationEndpoint struct {
	APIVersion string          `json:"apiVersion"`
	Kind       string          `json:"kind"`
	Metadata   Metadata        `json:"metadata"`
	Spec       DriverConfig    `json:"spec"`
	Status     json.RawMessage `json:"status,omitempty"`
}
```

### NotificationEndpointList

NotificationEndpointList defines model for NotificationEndpointList.

```go
type NotificationEndpointList struct {
	GeneratedAt time.Time              `json:"generatedAt,omitempty"`
	Items       []NotificationEndpoint `json:"items"`
	NextCursor  string                 `json:"nextCursor,omitempty"`
	Snapshot    string                 `json:"snapshot,omitempty"`
}
```

### NotificationGroup

NotificationGroup defines model for NotificationGroup.

```go
type NotificationGroup struct {
	APIVersion string   `json:"apiVersion"`
	Kind       string   `json:"kind"`
	Metadata   Metadata `json:"metadata"`

	// Spec Ordered endpoint and recipient references. At least one member is required; groups cannot contain other groups.
	Spec   NotificationGroupSpec `json:"spec"`
	Status json.RawMessage       `json:"status,omitempty"`
}
```

### NotificationGroupList

NotificationGroupList defines model for NotificationGroupList.

```go
type NotificationGroupList struct {
	GeneratedAt time.Time           `json:"generatedAt,omitempty"`
	Items       []NotificationGroup `json:"items"`
	NextCursor  string              `json:"nextCursor,omitempty"`
	Snapshot    string              `json:"snapshot,omitempty"`
}
```

### NotificationGroupSpec

NotificationGroupSpec Ordered endpoint and recipient references. At least one member is required; groups cannot contain other groups.

```go
type NotificationGroupSpec struct {
	EndpointRefs  []string `json:"endpointRefs,omitempty"`
	RecipientRefs []string `json:"recipientRefs,omitempty"`
}
```

### Operation

Operation defines model for Operation.

```go
type Operation struct {
	// Applied Optional observed progress; omission means not reported. Explicit zero or false is retained. Null is unsupported.
	Applied *int64 `json:"applied,omitempty"`

	// Committed Optional observed progress; omission means not reported. Explicit zero or false is retained. Null is unsupported.
	Committed     *int64 `json:"committed,omitempty"`
	ContentDigest string `json:"contentDigest"`

	// ExecutionResult Separate result availability, independent of Operation.state. Omission means the server has not reported execution-result support for this operation. Errors remain typed problems.
	ExecutionResult *ExecutionResultAvailability `json:"executionResult,omitempty"`
	ID              string                       `json:"id"`

	// IdentityFormat Present for collection protocol receipts; the caller must recognize it before uploading, validating or activating.
	IdentityFormat OperationIdentityFormat `json:"identityFormat,omitempty"`

	// ItemCount Declared frozen collection size. Required by collection protocol receipts; absent for non-collection operations. Null is unsupported.
	ItemCount  *int64        `json:"itemCount,omitempty"`
	Items      []ApplyResult `json:"items,omitempty"`
	NextCursor string        `json:"nextCursor,omitempty"`

	// NormalizationProfile Original raw-file normalization contract when declared. Missing means no file profile was recorded. Unknown future values are read-only and must not be used to reinterpret the original inventory.
	NormalizationProfile string `json:"normalizationProfile,omitempty"`
	RetryAfterSeconds    int64  `json:"retryAfterSeconds,omitempty"`

	// State Parent progress/outcome. canceled and invalidated stop further decisions but do not imply executionResult.state is ready. Unknown future values remain read-only.
	State string `json:"state"`

	// Uploaded Optional observed progress; omission means not reported. Explicit zero or false is retained. Null is unsupported.
	Uploaded *int64 `json:"uploaded,omitempty"`

	// Validated Optional observed progress; omission means not reported. Explicit zero or false is retained. Null is unsupported.
	Validated *bool `json:"validated,omitempty"`
}
```

### OperationCreateRequest.Format

```go
func (r OperationCreateRequest) Format(w fmt.State, _ rune)
```

### OperationCreateRequest.GoString

```go
func (r OperationCreateRequest) GoString() string
```

### OperationCreateRequest.String

String omits the write-only collection key and source commitment.

```go
func (OperationCreateRequest) String() string
```

### OperationCreateRequest

OperationCreateRequest defines model for OperationCreateRequest.

```go
type OperationCreateRequest struct {
	// AdmissionTicket Original opaque ticket returned by PrepareCollection. Keep it with the frozen input. Never replace it automatically after attempting Create. Expired or restored-epoch tickets cannot allocate new operations.
	AdmissionTicket string `json:"admissionTicket"`

	// ContentDigest Opaque keyed inventory commitment; requires the original frozen key and bytes to resume.
	ContentDigest string `json:"contentDigest"`

	// IdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.
	IdentityFormat OperationCreateRequestIdentityFormat `json:"identityFormat"`

	// IdentityKey Private per-frozen-collection HMAC key. Send only to the configured authenticated server. Never return, log or persist unencrypted.
	IdentityKey *string `json:"identityKey,omitempty"`
	ItemCount   int64   `json:"itemCount"`

	// NormalizationProfile Optional versioned raw-file normalization contract, bound to the original admission and encrypted collection identity. Omit for unprofiled or typed-resource inputs. This field alone does not enable source reselection.
	NormalizationProfile string `json:"normalizationProfile,omitempty"`

	// SourceFingerprint Private keyed source commitment. Source paths and URLs remain client-local.
	SourceFingerprint *string `json:"sourceFingerprint,omitempty"`
}
```

### OperationCreateRequestIdentityFormat.Valid

Valid indicates whether the value is a known member of the OperationCreateRequestIdentityFormat enum.

```go
func (e OperationCreateRequestIdentityFormat) Valid() bool
```

### OperationCreateRequestIdentityFormat

OperationCreateRequestIdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.

```go
type OperationCreateRequestIdentityFormat string
```

### OperationCreateRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1

Defines values for OperationCreateRequestIdentityFormat.

```go
const (
	OperationCreateRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1 OperationCreateRequestIdentityFormat = "cpra.collection.hmac-sha256-json-bytes.v1"
)
```

### OperationIdentityFormat.Valid

Valid indicates whether the value is a known member of the OperationIdentityFormat enum.

```go
func (e OperationIdentityFormat) Valid() bool
```

### OperationIdentityFormat

OperationIdentityFormat Present for collection protocol receipts; the caller must recognize it before uploading, validating or activating.

```go
type OperationIdentityFormat string
```

### OperationIdentityFormatCpraCollectionHmacSha256JsonBytesV1

Defines values for OperationIdentityFormat.

```go
const (
	OperationIdentityFormatCpraCollectionHmacSha256JsonBytesV1 OperationIdentityFormat = "cpra.collection.hmac-sha256-json-bytes.v1"
)
```

### OperationList

OperationList defines model for OperationList.

```go
type OperationList struct {
	GeneratedAt time.Time   `json:"generatedAt,omitempty"`
	Items       []Operation `json:"items"`
	NextCursor  string      `json:"nextCursor,omitempty"`
	Snapshot    string      `json:"snapshot,omitempty"`
}
```

### Outcome

Build requirement: `externaljobs`.

Outcome defines model for Outcome.

```go
type Outcome struct {
	Data                json.RawMessage `json:"data"`
	Diagnostic          string          `json:"diagnostic"`
	DiagnosticTruncated bool            `json:"diagnosticTruncated"`
	Evidence            []string        `json:"evidence"`
	ExecutionID         string          `json:"executionID"`
	GrantID             string          `json:"grantID"`
	Kind                string          `json:"kind"`
	RejectionCode       string          `json:"rejectionCode"`
	ServerID            string          `json:"serverID"`
	Status              string          `json:"status"`
	WorkerUID           string          `json:"workerUID"`
}
```

### Percentiles

Percentiles defines model for Percentiles.

```go
type Percentiles struct {
	Available bool    `json:"available"`
	P50MS     float64 `json:"p50Ms"`
	P95MS     float64 `json:"p95Ms"`
	P99MS     float64 `json:"p99Ms"`
}
```

### Pointer

Pointer returns a pointer to a copy of value, preserving optional false and
zero values without losing presence.

```go
func Pointer[T any](value T) *T
```

### PollRequest

Build requirement: `externaljobs`.

PollRequest defines model for PollRequest.

```go
type PollRequest struct {
	Capabilities    []WorkerCapability `json:"capabilities"`
	Capacity        int64              `json:"capacity"`
	ClientSessionID string             `json:"clientSessionID"`
	Limit           int64              `json:"limit"`
	PollSequence    int64              `json:"pollSequence"`
	ServerID        string             `json:"serverID"`
	SessionID       string             `json:"sessionID"`
	WaitSeconds     int64              `json:"waitSeconds"`
	WorkerID        string             `json:"workerID"`
}
```

### Pool

Pool defines model for Pool.

```go
type Pool struct {
	AdjustmentReason             string      `json:"adjustmentReason,omitempty"`
	Available                    bool        `json:"available"`
	Busy                         int64       `json:"busy,omitempty"`
	BusyAvailable                bool        `json:"busyAvailable"`
	Capacity                     int64       `json:"capacity"`
	LastScaleAt                  *time.Time  `json:"lastScaleAt,omitempty"`
	Maximum                      int64       `json:"maximum"`
	Minimum                      int64       `json:"minimum"`
	ModelRecommendation          int64       `json:"modelRecommendation,omitempty"`
	ModelRecommendationAvailable bool        `json:"modelRecommendationAvailable"`
	Name                         string      `json:"name"`
	PendingResults               int64       `json:"pendingResults"`
	ScalingEvents                int64       `json:"scalingEvents"`
	ServiceSamples               int64       `json:"serviceSamples"`
	ServiceTime                  Measurement `json:"serviceTime,omitempty"`
	SizingModel                  string      `json:"sizingModel"`
	SloCondition                 string      `json:"sloCondition"`
	Target                       int64       `json:"target"`
	TasksCompleted               int64       `json:"tasksCompleted"`
	TasksSubmitted               int64       `json:"tasksSubmitted"`
	WaitingTasks                 int64       `json:"waitingTasks"`

	// Workers Current worker goroutines, including idle workers; not a count of busy executions.
	Workers int64 `json:"workers"`
}
```

### PoolList

PoolList defines model for PoolList.

```go
type PoolList struct {
	Available   bool      `json:"available"`
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []Pool    `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### Preflight

Preflight defines model for Preflight.

```go
type Preflight struct {
	ContentDigest string       `json:"contentDigest,omitempty"`
	Errors        []FieldError `json:"errors,omitempty"`

	// IdentityFormat Present for collection protocol receipts; the caller must recognize it before uploading, validating or activating.
	IdentityFormat PreflightIdentityFormat `json:"identityFormat,omitempty"`

	// ItemCount Declared frozen collection size. Required by collection protocol receipts; absent for non-collection operations. Null is unsupported.
	ItemCount *int64        `json:"itemCount,omitempty"`
	Items     []ApplyResult `json:"items,omitempty"`
	Valid     bool          `json:"valid"`
}
```

### PreflightIdentityFormat.Valid

Valid indicates whether the value is a known member of the PreflightIdentityFormat enum.

```go
func (e PreflightIdentityFormat) Valid() bool
```

### PreflightIdentityFormat

PreflightIdentityFormat Present for collection protocol receipts; the caller must recognize it before uploading, validating or activating.

```go
type PreflightIdentityFormat string
```

### PreflightIdentityFormatCpraCollectionHmacSha256JsonBytesV1

Defines values for PreflightIdentityFormat.

```go
const (
	PreflightIdentityFormatCpraCollectionHmacSha256JsonBytesV1 PreflightIdentityFormat = "cpra.collection.hmac-sha256-json-bytes.v1"
)
```

### PreflightRequest.Format

```go
func (r PreflightRequest) Format(w fmt.State, _ rune)
```

### PreflightRequest.GoString

```go
func (r PreflightRequest) GoString() string
```

### PreflightRequest.String

Collection identity requests include private key material and may also include
provider configuration. Ordinary Go formatting deliberately omits the whole
request. JSON encoding remains available for the authenticated wire protocol;
callers must never log or persist that encoding as plaintext.

```go
func (PreflightRequest) String() string
```

### PreflightRequest

PreflightRequest defines model for PreflightRequest.

```go
type PreflightRequest struct {
	// ContentDigest Opaque keyed inventory commitment; requires the original frozen key and bytes to resume.
	ContentDigest string `json:"contentDigest"`

	// IdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.
	IdentityFormat PreflightRequestIdentityFormat `json:"identityFormat"`

	// IdentityKey Private per-frozen-collection HMAC key. Send only to the configured authenticated server. Never return, log or persist unencrypted.
	IdentityKey *string     `json:"identityKey,omitempty"`
	ItemCount   int64       `json:"itemCount"`
	Items       []ApplyItem `json:"items"`

	// SourceFingerprint Private keyed source commitment. Source paths and URLs remain client-local.
	SourceFingerprint *string `json:"sourceFingerprint,omitempty"`
}
```

### PreflightRequestIdentityFormat.Valid

Valid indicates whether the value is a known member of the PreflightRequestIdentityFormat enum.

```go
func (e PreflightRequestIdentityFormat) Valid() bool
```

### PreflightRequestIdentityFormat

PreflightRequestIdentityFormat Versioned commitment to exact transmitted resource-object bytes. No JSON canonicalization is performed.

```go
type PreflightRequestIdentityFormat string
```

### PreflightRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1

Defines values for PreflightRequestIdentityFormat.

```go
const (
	PreflightRequestIdentityFormatCpraCollectionHmacSha256JsonBytesV1 PreflightRequestIdentityFormat = "cpra.collection.hmac-sha256-json-bytes.v1"
)
```

### Problem

Problem defines model for Problem.

```go
type Problem struct {
	Code        string       `json:"code,omitempty"`
	Detail      string       `json:"detail,omitempty"`
	Errors      []FieldError `json:"errors,omitempty"`
	Instance    string       `json:"instance,omitempty"`
	OperationID string       `json:"operationID,omitempty"`
	RequestID   string       `json:"requestID,omitempty"`
	Status      int64        `json:"status,omitempty"`
	Title       string       `json:"title,omitempty"`
	Type        string       `json:"type,omitempty"`
}
```

### PulseDNSConfig

PulseDNSConfig defines model for PulseDNSConfig.

```go
type PulseDNSConfig struct {
	Host    *string `json:"host,omitempty"`
	Retries *int64  `json:"retries,omitempty"`
	Server  *string `json:"server,omitempty"`
}
```

### PulseDockerConfig

PulseDockerConfig defines model for PulseDockerConfig.

```go
type PulseDockerConfig struct {
	Container *string `json:"container,omitempty"`
	Retries   *int64  `json:"retries,omitempty"`
}
```

### PulseGRPCConfig

PulseGRPCConfig defines model for PulseGRPCConfig.

```go
type PulseGRPCConfig struct {
	Host    *string `json:"host,omitempty"`
	Port    *int64  `json:"port,omitempty"`
	Retries *int64  `json:"retries,omitempty"`
	Service *string `json:"service,omitempty"`
}
```

### PulseHTTPConfig

PulseHTTPConfig defines model for PulseHTTPConfig.

```go
type PulseHTTPConfig struct {
	Body               *string            `json:"body,omitempty"`
	ExpectedStatus     *[]int64           `json:"expectedStatus,omitempty"`
	Headers            *map[string]string `json:"headers,omitempty"`
	InsecureSkipVerify *bool              `json:"insecureSkipVerify,omitempty"`
	Method             *string            `json:"method,omitempty"`
	Retries            *int64             `json:"retries,omitempty"`
	URL                *string            `json:"url,omitempty"`
}
```

### PulseICMPConfig

PulseICMPConfig defines model for PulseICMPConfig.

```go
type PulseICMPConfig struct {
	Count     *int64  `json:"count,omitempty"`
	Host      *string `json:"host,omitempty"`
	Privilege *bool   `json:"ignorePrivilege,omitempty"`
	Retries   *int64  `json:"retries,omitempty"`
}
```

### PulseKafkaConfig

PulseKafkaConfig defines model for PulseKafkaConfig.

```go
type PulseKafkaConfig struct {
	Brokers *[]string `json:"brokers,omitempty"`
	Retries *int64    `json:"retries,omitempty"`
}
```

### PulseMongoConfig

PulseMongoConfig defines model for PulseMongoConfig.

```go
type PulseMongoConfig struct {
	Retries *int64  `json:"retries,omitempty"`
	URI     *string `json:"uri,omitempty"`
}
```

### PulseMySQLConfig

PulseMySQLConfig defines model for PulseMySQLConfig.

```go
type PulseMySQLConfig struct {
	Database *string `json:"database,omitempty"`
	DSN      *string `json:"dsn,omitempty"`
	Host     *string `json:"host,omitempty"`
	Password *string `json:"password,omitempty"`
	Port     *int64  `json:"port,omitempty"`
	Retries  *int64  `json:"retries,omitempty"`
	User     *string `json:"user,omitempty"`
}
```

### PulsePostgresConfig

PulsePostgresConfig defines model for PulsePostgresConfig.

```go
type PulsePostgresConfig struct {
	Database *string `json:"database,omitempty"`
	DSN      *string `json:"dsn,omitempty"`
	Host     *string `json:"host,omitempty"`
	Password *string `json:"password,omitempty"`
	Port     *int64  `json:"port,omitempty"`
	Retries  *int64  `json:"retries,omitempty"`
	SSLMode  *string `json:"sslmode,omitempty"`
	User     *string `json:"user,omitempty"`
}
```

### PulseRabbitMQConfig

PulseRabbitMQConfig defines model for PulseRabbitMQConfig.

```go
type PulseRabbitMQConfig struct {
	Retries *int64  `json:"retries,omitempty"`
	URL     *string `json:"url,omitempty"`
}
```

### PulseRedisConfig

PulseRedisConfig defines model for PulseRedisConfig.

```go
type PulseRedisConfig struct {
	Addr     *string `json:"addr,omitempty"`
	DB       *int64  `json:"db,omitempty"`
	Password *string `json:"password,omitempty"`
	Retries  *int64  `json:"retries,omitempty"`
	Username *string `json:"username,omitempty"`
}
```

### PulseTCPConfig

PulseTCPConfig defines model for PulseTCPConfig.

```go
type PulseTCPConfig struct {
	Host    *string `json:"host,omitempty"`
	Port    *int64  `json:"port,omitempty"`
	Retries *int64  `json:"retries,omitempty"`
}
```

### PulseTLSConfig

PulseTLSConfig defines model for PulseTLSConfig.

```go
type PulseTLSConfig struct {
	CriticalDays       *int64  `json:"criticalDays,omitempty"`
	Host               *string `json:"host,omitempty"`
	InsecureSkipVerify *bool   `json:"insecureSkipVerify,omitempty"`
	Port               *int64  `json:"port,omitempty"`
	Retries            *int64  `json:"retries,omitempty"`
	ServerName         *string `json:"serverName,omitempty"`
	WarnDays           *int64  `json:"warnDays,omitempty"`
}
```

### PulseUDPConfig

PulseUDPConfig defines model for PulseUDPConfig.

```go
type PulseUDPConfig struct {
	Host    *string `json:"host,omitempty"`
	Payload *string `json:"payload,omitempty"`
	Port    *int64  `json:"port,omitempty"`
	Retries *int64  `json:"retries,omitempty"`
}
```

### Queue

Queue defines model for Queue.

```go
type Queue struct {
	Available   bool        `json:"available"`
	AverageWait Measurement `json:"averageWait,omitempty"`
	Capacity    int64       `json:"capacity"`

	// CompletedPerSecond Absent when completed-execution rate is not measured; consult completedRateAvailable.
	CompletedPerSecond     float64 `json:"completedPerSecond,omitempty"`
	CompletedRateAvailable bool    `json:"completedRateAvailable"`
	Depth                  int64   `json:"depth"`

	// DequeuedPerSecond Dequeued jobs per second; this is not a completed-execution rate.
	DequeuedPerSecond float64     `json:"dequeuedPerSecond"`
	Drops             int64       `json:"drops"`
	EnqueuedPerSecond float64     `json:"enqueuedPerSecond"`
	LimitReason       string      `json:"limitReason,omitempty"`
	MaximumWait       Measurement `json:"maximumWait,omitempty"`
	Name              string      `json:"name"`
	OldestPendingMS   Measurement `json:"oldestPendingMs,omitempty"`
	RatesAvailable    bool        `json:"ratesAvailable"`

	// SampleWindow Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	SampleWindow Duration    `json:"sampleWindow,omitempty"`
	Saturated    bool        `json:"saturated"`
	Utilization  Measurement `json:"utilization,omitempty"`
}
```

### QueueList

QueueList defines model for QueueList.

```go
type QueueList struct {
	Available   bool      `json:"available"`
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []Queue   `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### Receipt

Build requirement: `externaljobs`.

Receipt defines model for Receipt.

```go
type Receipt struct {
	Disposition ReceiptDisposition `json:"disposition"`
	ExecutionID string             `json:"executionID"`
	ReceiptID   string             `json:"receiptID"`
	ServerID    string             `json:"serverID"`
	WorkerUID   string             `json:"workerUID"`
}
```

### ReceiptDisposition.Valid

Build requirement: `externaljobs`.

Valid indicates whether the value is a known member of the ReceiptDisposition enum.

```go
func (e ReceiptDisposition) Valid() bool
```

### ReceiptDisposition

Build requirement: `externaljobs`.

ReceiptDisposition defines model for ReceiptDisposition.

```go
type ReceiptDisposition string
```

### ReceiptDispositionAccepted ReceiptDispositionFinalized ReceiptDispositionUnknown

Build requirement: `externaljobs`.

Defines values for ReceiptDisposition.

```go
const (
	ReceiptDispositionAccepted  ReceiptDisposition = "accepted"
	ReceiptDispositionFinalized ReceiptDisposition = "finalized"
	ReceiptDispositionUnknown   ReceiptDisposition = "unknown"
)
```

### Recipient

Recipient defines model for Recipient.

```go
type Recipient struct {
	APIVersion string   `json:"apiVersion"`
	Kind       string   `json:"kind"`
	Metadata   Metadata `json:"metadata"`

	// Spec A notification contact with ordered references to typed notification endpoints; not an authentication principal.
	Spec   RecipientSpec   `json:"spec"`
	Status json.RawMessage `json:"status,omitempty"`
}
```

### RecipientList

RecipientList defines model for RecipientList.

```go
type RecipientList struct {
	GeneratedAt time.Time   `json:"generatedAt,omitempty"`
	Items       []Recipient `json:"items"`
	NextCursor  string      `json:"nextCursor,omitempty"`
	Snapshot    string      `json:"snapshot,omitempty"`
}
```

### RecipientSpec

RecipientSpec A notification contact with ordered references to typed notification endpoints; not an authentication principal.

```go
type RecipientSpec struct {
	EndpointRefs []string `json:"endpointRefs"`
}
```

### RecoverySpec

RecoverySpec defines model for RecoverySpec.

```go
type RecoverySpec struct {
	// Cooldown Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Cooldown    *Duration    `json:"cooldown,omitempty"`
	Driver      DriverConfig `json:"driver"`
	MaxAttempts *int64       `json:"maxAttempts,omitempty"`
	MaxFailures *int64       `json:"maxFailures,omitempty"`
	Retries     *int64       `json:"retries,omitempty"`
}
```

### Resource

Resource defines model for Resource.

```go
type Resource struct {
	APIVersion string          `json:"apiVersion"`
	Kind       string          `json:"kind"`
	Metadata   Metadata        `json:"metadata"`
	Spec       json.RawMessage `json:"spec"`
	Status     json.RawMessage `json:"status,omitempty"`
}
```

### RuntimeConfig

RuntimeConfig defines model for RuntimeConfig.

```go
type RuntimeConfig struct {
	Available bool `json:"available"`

	// HistoryRetention Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	HistoryRetention Duration `json:"historyRetention,omitempty"`
	QueueCapacity    int64    `json:"queueCapacity,omitempty"`

	// QueueTarget Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	QueueTarget Duration `json:"queueTarget,omitempty"`
	ReadOnly    bool     `json:"readOnly"`

	// ResultTarget Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	ResultTarget Duration `json:"resultTarget,omitempty"`

	// SloWindow Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	SloWindow   Duration `json:"sloWindow,omitempty"`
	StorageMode string   `json:"storageMode,omitempty"`
}
```

### SLOReport

SLOReport defines model for SLOReport.

```go
type SLOReport struct {
	// Attainment Compatibility alias for resultAttainment, a fraction from 0 to 1; unavailable when no expectations were recorded.
	Attainment Measurement `json:"attainment,omitempty"`
	Condition  string      `json:"condition,omitempty"`
	Driver     string      `json:"driver"`
	Execution  Percentiles `json:"execution,omitempty"`
	Expected   int64       `json:"expected"`
	Missed     int64       `json:"missed"`
	Overdue    int64       `json:"overdue"`

	// PausedMonitorSeconds Intentional pause exposure inside this rolling window, in monitor-seconds; this is a gauge, not a monotonic counter.
	PausedMonitorSeconds float64 `json:"pausedMonitorSeconds"`

	// PausedMonitors Current owner-observed intentionally paused monitor count; not missed checks.
	PausedMonitors       int64       `json:"pausedMonitors"`
	Pending              int64       `json:"pending"`
	Pipeline             string      `json:"pipeline"`
	QueueAttainment      Measurement `json:"queueAttainment,omitempty"`
	QueueDelay           Percentiles `json:"queueDelay,omitempty"`
	QueueThresholdPassed int64       `json:"queueThresholdPassed"`
	ResultAttainment     Measurement `json:"resultAttainment,omitempty"`
	Samples              int64       `json:"samples"`
	Timeouts             int64       `json:"timeouts"`
	TotalLatency         Percentiles `json:"totalLatency,omitempty"`
	TotalThresholdPassed int64       `json:"totalThresholdPassed"`
}
```

### SLOView

SLOView defines model for SLOView.

```go
type SLOView struct {
	Available      bool        `json:"available"`
	CoverageGap    bool        `json:"coverageGap"`
	GapEnd         *time.Time  `json:"gapEnd,omitempty"`
	GapStart       *time.Time  `json:"gapStart,omitempty"`
	GeneratedAt    time.Time   `json:"generatedAt"`
	QueueTargetMS  float64     `json:"queueTargetMs"`
	Reports        []SLOReport `json:"reports"`
	ResultTargetMS float64     `json:"resultTargetMs"`

	// Window Go duration string, e.g. 60s. Never numeric nanoseconds or ISO-8601.
	Window Duration `json:"window"`
}
```

### Schema

Schema returns an independent copy of this build's OpenAPI contract.

```go
func Schema() []byte
```

### StartDisposition.Valid

Build requirement: `externaljobs`.

Valid indicates whether the value is a known member of the StartDisposition enum.

```go
func (e StartDisposition) Valid() bool
```

### StartDisposition

Build requirement: `externaljobs`.

StartDisposition defines model for StartDisposition.

```go
type StartDisposition string
```

### StartDispositionGranted StartDispositionPending StartDispositionRejected StartDispositionStarted StartDispositionTerminal StartDispositionUnknown

Build requirement: `externaljobs`.

Defines values for StartDisposition.

```go
const (
	StartDispositionGranted  StartDisposition = "granted"
	StartDispositionPending  StartDisposition = "pending"
	StartDispositionRejected StartDisposition = "rejected"
	StartDispositionStarted  StartDisposition = "started"
	StartDispositionTerminal StartDisposition = "terminal"
	StartDispositionUnknown  StartDisposition = "unknown"
)
```

### StartMode.Valid

Build requirement: `externaljobs`.

Valid indicates whether the value is a known member of the StartMode enum.

```go
func (e StartMode) Valid() bool
```

### StartMode

Build requirement: `externaljobs`.

StartMode defines model for StartMode.

```go
type StartMode string
```

### StartModeBegin StartModeReconcile

Build requirement: `externaljobs`.

Defines values for StartMode.

```go
const (
	StartModeBegin     StartMode = "begin"
	StartModeReconcile StartMode = "reconcile"
)
```

### StartRequest

Build requirement: `externaljobs`.

StartRequest defines model for StartRequest.

```go
type StartRequest struct {
	ExecutionID       string    `json:"executionID"`
	ExecutionRevision string    `json:"executionRevision"`
	LeaseID           string    `json:"leaseID"`
	Mode              StartMode `json:"mode"`
	ServerID          string    `json:"serverID"`
	SessionID         string    `json:"sessionID"`
}
```

### StartResponse

Build requirement: `externaljobs`.

StartResponse Only granted from the original live begin permits invocation. Reconcile never grants. Other dispositions report original attempt state with conditional grant and receipt fields.

```go
type StartResponse struct {
	Deadline          time.Time        `json:"deadline"`
	Disposition       StartDisposition `json:"disposition"`
	ExecutionID       string           `json:"executionID"`
	ExecutionRevision string           `json:"executionRevision"`
	GrantID           string           `json:"grantID"`
	LeaseID           string           `json:"leaseID"`
	ReceiptID         string           `json:"receiptID"`
	ServerID          string           `json:"serverID"`
	SessionID         string           `json:"sessionID"`
	WorkerUID         string           `json:"workerUID"`
}
```

### State

State defines model for State.

```go
type State struct {
	Actions                 []Action     `json:"actions,omitempty"`
	ControllerAvailable     bool         `json:"controllerAvailable"`
	ControllerReady         bool         `json:"controllerReady"`
	ControllerReason        string       `json:"controllerReason"`
	GeneratedAt             time.Time    `json:"generatedAt"`
	Live                    bool         `json:"live"`
	ProjectionAgeMS         float64      `json:"projectionAgeMs,omitempty"`
	ProjectionAvailable     bool         `json:"projectionAvailable"`
	ProjectionFresh         bool         `json:"projectionFresh"`
	ReadinessReason         string       `json:"readinessReason,omitempty"`
	Ready                   bool         `json:"ready"`
	Storage                 StorageState `json:"storage"`
	UnknownActions          int64        `json:"unknownActions,omitempty"`
	UnknownActionsAvailable bool         `json:"unknownActionsAvailable"`
}
```

### StorageState

StorageState defines model for StorageState.

```go
type StorageState struct {
	AppliedIndex int64 `json:"appliedIndex"`

	// Available Current durable-store observation.
	Available      bool  `json:"available"`
	Bytes          int64 `json:"bytes,omitempty"`
	BytesAvailable bool  `json:"bytesAvailable"`

	// CommitIndex Compatibility observation of the FSM-applied log index; not an independently sampled Raft commit index.
	CommitIndex   int64       `json:"commitIndex,omitempty"`
	CommitLatency Measurement `json:"commitLatency,omitempty"`
	Error         string      `json:"error,omitempty"`

	// FormatVersion Current application snapshot format, distinct from node identity and authentication format versions.
	FormatVersion int64  `json:"formatVersion,omitempty"`
	Mode          string `json:"mode"`

	// SingleNode Current durable-store observation.
	SingleNode       bool        `json:"singleNode"`
	SnapshotDuration Measurement `json:"snapshotDuration,omitempty"`
}
```

### StrictDecode

StrictDecode rejects duplicate keys, unknown typed fields and trailing values.

```go
func StrictDecode(data []byte, out any) error
```

### System

System defines model for System.

```go
type System struct {
	Available             bool        `json:"available"`
	AverageUpdateDuration Measurement `json:"averageUpdateDuration,omitempty"`
	EntitiesProcessed     int64       `json:"entitiesProcessed"`
	LastUpdateDurationMS  Measurement `json:"lastUpdateDurationMs,omitempty"`
	MaximumUpdateDuration Measurement `json:"maximumUpdateDuration,omitempty"`
	MinimumUpdateDuration Measurement `json:"minimumUpdateDuration,omitempty"`
	Name                  string      `json:"name"`
	ProgressAt            *time.Time  `json:"progressAt,omitempty"`
	Updates               int64       `json:"updates"`
}
```

### SystemList

SystemList defines model for SystemList.

```go
type SystemList struct {
	Available   bool      `json:"available"`
	GeneratedAt time.Time `json:"generatedAt,omitempty"`
	Items       []System  `json:"items"`
	NextCursor  string    `json:"nextCursor,omitempty"`
	Snapshot    string    `json:"snapshot,omitempty"`
}
```

### UploadRequest

UploadRequest defines model for UploadRequest.

```go
type UploadRequest struct {
	Items []ApplyItem `json:"items"`
}
```

### ValidateDriver

ValidateDriver rejects unknown variants on mutation; ordinary read decoding
retains their type and raw configuration for inspection.

```go
func ValidateDriver(category string, d DriverConfig) error
```

### ValidateExecutionResult

ValidateExecutionResult checks a bounded observation, not activation authority.
Unknown observation vocabulary remains readable without assuming its meaning.

```go
func ValidateExecutionResult(p Operation) error
```

### ValidateExecutionResultMetadata

ValidateExecutionResultMetadata validates an operation receipt containing
availability, counts and an optional summary without an execution item page.
Activation receipts use this shape even after the retained result is ready.

```go
func ValidateExecutionResultMetadata(p Operation) error
```

### ValidateResource

ValidateResource checks a resource envelope, spec, and selected driver shapes.
It performs no server authorization, live dependency check, or provider I/O.

```go
func ValidateResource(r Resource) error
```

### ValidateResourceValue

ValidateResourceValue validates a typed resource before mutation.

```go
func ValidateResourceValue(v any) error
```

### ValidateValidationResultPage

ValidateValidationResultPage checks a bounded read observation without granting
activation authority. Unknown issue/change/identity-format strings remain
readable; a mutation helper must separately recognize its supported contract.

```go
func ValidateValidationResultPage(p ValidationResultPage) error
```

### ValidationResultItem

ValidationResultItem Retained, safe validation metadata in original input order. Contains no resource bodies or credentials.

```go
type ValidationResultItem struct {
	// Change Known changes: create, update, unchanged. Unknown values are read-only observations.
	Change string `json:"change,omitempty"`
	ID     string `json:"id"`

	// Issue Allowlisted issue code, never a provider diagnostic or source path. Unknown codes are read-only observations.
	Issue           string `json:"issue,omitempty"`
	Kind            string `json:"kind"`
	Ordinal         int64  `json:"ordinal"`
	ResourceVersion string `json:"resourceVersion,omitempty"`

	// Source Opaque ordered source token; never a filename or URL.
	Source         string `json:"source"`
	SourceDocument int64  `json:"sourceDocument"`
	SourceItem     int64  `json:"sourceItem"`
	UID            string `json:"uid,omitempty"`
}
```

### ValidationResultPage

ValidationResultPage A bounded page from one sealed validation result. Pending validation is a problem response, never valid=false. At most 4 MiB encoded response; default 100 rows, maximum 500.

```go
type ValidationResultPage struct {
	ContentDigest  string                 `json:"contentDigest"`
	IdentityFormat string                 `json:"identityFormat"`
	ItemCount      int64                  `json:"itemCount"`
	Items          []ValidationResultItem `json:"items"`
	NextCursor     string                 `json:"nextCursor,omitempty"`
	OperationID    string                 `json:"operationID"`

	// Summary Immutable original validation verdict. Count is retained result rows, distinct from declared input itemCount. A successful plan is not permission to execute.
	Summary ValidationResultSummary `json:"summary"`
}
```

### ValidationResultSummary

ValidationResultSummary Immutable original validation verdict. Count is retained result rows, distinct from declared input itemCount. A successful plan is not permission to execute.

```go
type ValidationResultSummary struct {
	CapabilitiesDigest string    `json:"capabilitiesDigest"`
	Count              int64     `json:"count"`
	Digest             string    `json:"digest"`
	ExpiresAt          time.Time `json:"expiresAt"`
	FinalizedAt        time.Time `json:"finalizedAt"`
	Issue              string    `json:"issue,omitempty"`
	PlanDigest         string    `json:"planDigest,omitempty"`
	PlanID             string    `json:"planID,omitempty"`
	ResultID           string    `json:"resultID"`
	SummaryOnly        bool      `json:"summaryOnly"`
	Valid              bool      `json:"valid"`
}
```

### Version

Version defines model for Version.

```go
type Version struct {
	Commit        string    `json:"commit,omitempty"`
	GoVersion     string    `json:"goVersion,omitempty"`
	SourceTime    time.Time `json:"sourceTime,omitempty"`
	StorageFormat int64     `json:"storageFormat,omitempty"`
	Version       string    `json:"version"`
}
```

### WorkerCapability

Build requirement: `externaljobs`.

WorkerCapability defines model for WorkerCapability.

```go
type WorkerCapability struct {
	JobTypeID string `json:"jobTypeID"`
	Kind      string `json:"kind"`
	Version   string `json:"version"`
}
```

### WorkerObservation

Build requirement: `externaljobs`.

WorkerObservation defines model for WorkerObservation.

```go
type WorkerObservation struct {
	Active     int64     `json:"active"`
	Capacity   int64     `json:"capacity"`
	LastSeenAt time.Time `json:"lastSeenAt"`
	WorkerID   string    `json:"workerID"`
}
```

### WorkerObservationList

Build requirement: `externaljobs`.

WorkerObservationList defines model for WorkerObservationList.

```go
type WorkerObservationList struct {
	Items      []WorkerObservation `json:"items"`
	NextCursor string              `json:"nextCursor,omitempty"`
}
```

## Package collection

### *Frozen.Close

Close closes and removes private plaintext staging. Calling it again is safe.

```go
func (f *Frozen) Close() error
```

### *Frozen.Digest

Digest returns the frozen collection's content identity for operation resume.

```go
func (f *Frozen) Digest() string
```

### *Frozen.Item

Item reads one bounded resource. Concurrent reads are supported until Close.

```go
func (f *Frozen) Item(ctx context.Context, index int) (Item, error)
```

### *Frozen.Len

Len returns the number of frozen resource records.

```go
func (f *Frozen) Len() int
```

### *Frozen.NormalizationProfile

NormalizationProfile identifies the immutable file-normalization contract.
Ordinary Freeze and FreezeResources return an empty profile. Closing a Frozen
does not change this public, non-secret identity metadata.

```go
func (f *Frozen) NormalizationProfile() string
```

### *Frozen.Range

Range reads at most one resource at a time in frozen source order.

```go
func (f *Frozen) Range(ctx context.Context, visit func(Item) error) error
```

### *Frozen.StagedBytes

StagedBytes reports encoded resource bytes retained in local staging.

```go
func (f *Frozen) StagedBytes() int64
```

### Apply

Apply creates inactive staging only after Freeze has fully parsed and locally
validated every source. It uploads bounded chunks, asks the server to validate
the entire staged union, and activates only after that succeeds. The server is
responsible for dependency ordering, authorization, and per-resource CAS.
Repeating Apply with the same Frozen reuses its original creation ticket,
including after an uncertain Create response or expiry. It never renews the
ticket automatically; create a new Frozen only for an intentional new attempt.

```go
func Apply(ctx context.Context, operations Operations, frozen *Frozen) (Result, error)
```

### Decode

Decode streams YAML/JSON resources and monitor manifests from r using Freeze's
parser, normalization and duplicate checks. It never opens a file or URL and
does not close r. Each callback owns its item and may encrypt it before staging.

A later malformed item can fail after earlier callbacks. Callbacks MUST stage
resources without activation when whole-input validation is required. A nil
return establishes parse completion, not cross-resource or server validation.
Use Freeze for ordinary clients that need a complete reusable plaintext spool.

```go
func Decode(ctx context.Context, r io.Reader, options DecodeOptions, visit func(Item) error) error
```

### DecodeOptions

DecodeOptions bounds decoding without writing a plaintext staging file.
The identity index retains at most MaxResources entries; only the current
document/resource body is decoded at a time. Zero values select the same
limits as Freeze. SourceName is a caller-supplied non-secret diagnostic label.

```go
type DecodeOptions struct {
	SourceName       string
	MaxBytes         int64
	MaxResourceBytes int
	MaxDocumentBytes int
	MaxResources     int
}
```

### Diff

Diff uses the same ephemeral preflight contract and returns proposed item
outcomes. It makes no active writes or implicit deletions.

```go
func Diff(ctx context.Context, operations Operations, frozen *Frozen) (*api.Preflight, error)
```

### ErrApplyInProgress

ErrApplyInProgress means this Frozen already has an active Apply or Resume.
No request from the competing call was submitted.

```go
var ErrApplyInProgress = errors.New("collection application is already in progress")
```

### ErrPreflightRejected

ErrPreflightRejected reports a server validation response whose Valid is false.

```go
var ErrPreflightRejected = errors.New("collection preflight rejected")
```

### ErrProgressUnavailable

ErrProgressUnavailable means the server did not report upload progress needed
to resume a mutable phase. No upload, validation or activation was submitted.

```go
var ErrProgressUnavailable = errors.New("operation upload progress is unavailable; no resume mutation was submitted")
```

### ErrReselectionObservation

ErrReselectionObservation means a response cannot establish the original
identity or monotone progress. Mutation responses also match cpra.ErrAmbiguous.

```go
var ErrReselectionObservation = errors.New("original upload recovery observation is invalid")
```

### ErrReselectionState

ErrReselectionState means the original operation or attempt cannot continue.
A failed attempt's allowlisted category remains in ReselectionResult.Attempt.

```go
var ErrReselectionState = errors.New("original upload cannot continue in its observed state")
```

### ErrUnsupportedNormalization

ErrUnsupportedNormalization identifies an unavailable normalization profile.
An absent profile is not silently interpreted as the current profile.

```go
var ErrUnsupportedNormalization = errors.New("unsupported collection normalization profile")
```

### ExecutionOperations

ExecutionOperations exposes only the read needed to await retained results.

```go
type ExecutionOperations interface {
	ExecutionResult(context.Context, string, cpra.ExecutionResultPageOptions) (*cpra.Response[api.Operation], error)
}
```

### File

File selects a file or directory for Freeze. Recursive traversal requires
Options.Recursive; this constructor does not read the filesystem.

```go
func File(path string) Source
```

### FileNormalizationProfile

FileNormalizationProfile identifies the versioned base file contract shared
by browser imports and server-assisted reselection. It is independent of the
inventory HMAC format, compiler version and source-build hash. External job
resources and drivers are excluded even when this package has externaljobs.

```go
const FileNormalizationProfile = "cpra.file.base.v1"
```

### Freeze

Freeze reads all inputs before returning. Any parse, duplicate, quota, or
cancellation error removes the complete staging directory. Remote permission,
capability, schema, and live-reference validation still require Preflight.

```go
func Freeze(ctx context.Context, sources []Source, options Options) (_ *Frozen, err error)
```

### FreezeProfile

FreezeProfile freezes explicitly selected files, directories, readers or URLs
using the named file-normalization contract. Only FileNormalizationProfile is
supported. Unsupported profiles fail before reading any source. This function
does not contact CPRa; Apply must be invoked separately after all input freezes.

Source order follows the explicit list and lexical directory traversal, with
duplicate file paths included once. Raw comments, whitespace and empty files
participate in the source commitment. Exact NormalizeFile resource bytes and
coordinates participate in the item commitment; files are never read again.

The base profile permits at most 1,000 expanded sources, 64 MiB cumulative raw
input, 10,000 resources, 1 MiB per resource, 16 MiB document metadata accounting
and 512 MiB plaintext staging. Options may tighten but never expand these
limits. Ordinary Freeze and FreezeResources keep their separate contracts.

```go
func FreezeProfile(ctx context.Context, sources []Source, profile string, options Options) (_ *Frozen, err error)
```

### FreezeResources

FreezeResources reads, validates, and privately stages a typed resource stream.
It returns only after all resources have been frozen; failure removes staging.

```go
func FreezeResources(ctx context.Context, source ResourceSource, options Options) (_ *Frozen, err error)
```

### Frozen.Format

```go
func (f Frozen) Format(w fmt.State, _ rune)
```

### Frozen.GoString

```go
func (f Frozen) GoString() string
```

### Frozen.MarshalJSON

```go
func (Frozen) MarshalJSON() ([]byte, error)
```

### Frozen.String

Formatting excludes private key material, source labels and staged resources.

```go
func (Frozen) String() string
```

### Frozen

Frozen holds private, plaintext, bounded client staging. Close removes all
staging. It is not a persistent resume token and must not be included in logs
or backups. A new Freeze observes changed files and generates a new identity;
this instance never does. Use the returned pointer and do not copy Frozen.

```go
type Frozen struct {
	// contains filtered or unexported fields
}
```

### Item.Format

```go
func (i Item) Format(w fmt.State, _ rune)
```

### Item.GoString

```go
func (i Item) GoString() string
```

### Item.String

```go
func (Item) String() string
```

### Item

Item is a desired resource. Frozen items carry a keyed ContentDigest and exact
inventory position; parser-only Decode items retain a local SHA-256 digest.
ID is the immutable identity within an apply operation, distinct from resource ID.

```go
type Item struct {
	ID            string              `json:"id"`
	ContentDigest string              `json:"contentDigest"`
	Location      Location            `json:"location"`
	Resource      api.Resource        `json:"resource"`
	Position      commitment.Position `json:"position"`
}
```

### Location.String

String formats a source location without including its resource payload.

```go
func (l Location) String() string
```

### Location

Location is retained without URL query strings, user credentials, or payloads.

```go
type Location struct {
	Source   string `json:"source"`
	Document int    `json:"document"`
	Item     int    `json:"item"`
}
```

### MaxChunkItems MaxChunkBytes

```go
const (
	// MaxChunkItems bounds the resource records in one upload request.
	MaxChunkItems = 256
	// MaxChunkBytes bounds an encoded upload or ephemeral preflight request.
	MaxChunkBytes = 4 << 20
)
```

### NormalizeFile

NormalizeFile streams one UTF-8 YAML/JSON source through the named profile.
It does not open, close, seek or stage the reader. The callback owns each
result, but must stage without activation: a later malformed item can fail
after earlier callbacks. A nil error proves parsing finished, not reference
validation, authorization or identity equality with an existing operation.

The base profile uses Decode's manifest normalization, document/item coordinates
and duplicate detection followed by encoding/json resource serialization.
Its ceilings are 64 MiB of raw source, 16 MiB for the decoder's document
metadata accounting, 1 MiB per resource and 10,000 resources. The document
counter measures YAML metadata lines or JSON non-list field-value bytes;
it does not measure a whole raw document or all decoded allocations. Lists
stream entries independently. Zero limits select those ceilings; nonzero
limits may tighten but never expand them.
SourceName is local diagnostic metadata and is not part of normalized JSON.
Ordinary Decode and Freeze retain their separate, configurable contracts.

```go
func NormalizeFile(ctx context.Context, r io.Reader, profile string, options DecodeOptions, visit func(NormalizedItem) error) error
```

### NormalizedItem.Format

```go
func (i NormalizedItem) Format(w fmt.State, _ rune)
```

### NormalizedItem.GoString

```go
func (i NormalizedItem) GoString() string
```

### NormalizedItem.MarshalJSON

```go
func (NormalizedItem) MarshalJSON() ([]byte, error)
```

### NormalizedItem.String

```go
func (NormalizedItem) String() string
```

### NormalizedItem

NormalizedItem contains the exact resource JSON and coordinates used by the
file profile. JSON belongs to the callback; callers must not serialize the
resource again before computing its inventory commitment. It may hold secrets.

```go
type NormalizedItem struct {
	ID       string
	Location Location
	JSON     []byte
}
```

### Operations

Operations is implemented by cpra.OperationsService. Keeping the interface
narrow also permits protocol contract tests without importing a server.

```go
type Operations interface {
	Preflight(context.Context, api.PreflightRequest) (*cpra.Response[api.Preflight], error)
	Prepare(context.Context, api.CollectionPrepareRequest) (*cpra.Response[api.CollectionAdmission], error)
	Create(context.Context, api.OperationCreateRequest) (*cpra.Response[api.Operation], error)
	Upload(context.Context, string, api.UploadRequest) (*cpra.Response[api.Operation], error)
	Validate(context.Context, string) (*cpra.Response[api.Operation], error)
	WaitValidation(context.Context, string) (*cpra.Response[api.ValidationResultPage], error)
	Activate(context.Context, string) (*cpra.Response[api.Operation], error)
	Get(context.Context, string) (*cpra.Response[api.Operation], error)
	Cancel(context.Context, string) (*cpra.Response[api.Operation], error)
}
```

### Options

Options bounds plaintext client staging. A zero field selects its default.
SourceTransport, when supplied, must be an unauthenticated source transport;
it must not inject API or provider credentials.

```go
type Options struct {
	Recursive       bool
	AllowHTTP       bool
	TempDir         string
	MaxStagingBytes int64
	// MaxSourceBytes bounds cumulative raw inputs, separately from peak staging.
	// Zero defaults to MaxStagingBytes. Typed streams count serialized objects plus LF.
	MaxSourceBytes   int64
	MaxResourceBytes int
	MaxDocumentBytes int
	MaxResources     int
	SourceTimeout    time.Duration
	SourceTransport  http.RoundTripper
}
```

### Preflight

Preflight performs ephemeral, bounded server validation/diff. It does not
create a durable apply operation. A collection larger than one 4 MiB request
fails explicitly: streaming ephemeral validation requires server support.
Apply can stage a larger collection in encrypted, inactive server storage.
The ephemeral contract carries exact item commitments without a file-profile
field. Durable Prepare/Create and operation observations bind that profile.

```go
func Preflight(ctx context.Context, operations Operations, frozen *Frozen) (*api.Preflight, error)
```

### Reader

Reader consumes r once; ownership and closing remain with its caller.

```go
func Reader(name string, r io.Reader) Source
```

### Reference

Reference identifies a dependency independently of its display name.

```go
type Reference struct{ Kind, ID string }
```

### Reselect

Reselect verifies selected raw files and completes only the missing upload
suffix under the server-held original identity. It never creates a replacement
collection, validates configuration, activates it or cancels server work.

All required raw inputs are privately frozen before creating an attempt or
uploading a part. Explicit source order and lexical directory expansion match
Freeze; retain the original source boundaries and include empty sources.
The supported original profile is FileNormalizationProfile. Ordinary unprofiled
Freeze/FreezeResources operations cannot be relabeled by this helper.

A supplied AttemptID is read before continuing its observed phase. Its already
staged raw bytes remain authoritative: selected sources supply only missing
bytes. Same-length edits to a previously staged prefix are not compared with
the newly selected prefix; the server proves its complete retained input
against the original collection identity before appending any resources.
Sources are needed only for an uploading attempt; verification and transfer
can finish without reopening files. An already full original upload returns Complete
without consulting a possibly disappeared attempt. Lost mutation replies stop
immediately: call again explicitly with the known attempt ID to reconcile.
A lost create reply without an attempt ID cannot be rediscovered by this API.
An initially observed failed/transfer_failed attempt may receive one explicit
Resume request; the server decides whether its retained transfer can continue.
A failure observed later in this invocation stops without retrying.
Polling is read-only, at least five seconds apart, and does not renew expiry.
Reader sources remain caller-owned; callers must interrupt blocking readers
themselves when their context is canceled.
Context cancellation stops this helper, never an admitted server operation.

```go
func Reselect(ctx context.Context, operations ReselectionOperations, operationID string, sources []Source, options ReselectionOptions) (result ReselectionResult, err error)
```

### ReselectionOperations

ReselectionOperations exposes only original-upload recovery. It deliberately
excludes collection creation, validation, activation, cancellation and discard.

```go
type ReselectionOperations interface {
	Get(context.Context, string) (*cpra.Response[api.Operation], error)
	CreateReselection(context.Context, string, api.CollectionReselectionCreateRequest) (*cpra.Response[api.CollectionReselectionAttempt], error)
	GetReselection(context.Context, string, string) (*cpra.Response[api.CollectionReselectionAttempt], error)
	UploadReselectionSource(context.Context, string, string, cpra.ReselectionSourcePart) (*cpra.Response[api.CollectionReselectionAttempt], error)
	VerifyReselection(context.Context, string, string) (*cpra.Response[api.CollectionReselectionAttempt], error)
	ResumeReselection(context.Context, string, string) (*cpra.Response[api.CollectionReselectionAttempt], error)
}
```

### ReselectionOptions

ReselectionOptions selects a known attempt and bounds raw source acquisition.
Sources uses Options' file expansion and separate unauthenticated URL client.
Raw source bytes never exceed 64 MiB; a smaller MaxSourceBytes or
MaxStagingBytes lowers that limit. Resource decoding remains server-owned.

```go
type ReselectionOptions struct {
	AttemptID string
	Sources   Options
}
```

### ReselectionResult.Format

```go
func (r ReselectionResult) Format(w fmt.State, _ rune)
```

### ReselectionResult.GoString

```go
func (r ReselectionResult) GoString() string
```

### ReselectionResult.String

```go
func (ReselectionResult) String() string
```

### ReselectionResult

ReselectionResult retains the original handle and last checked metadata on
failure. Complete means only that the original upload is full; it does not
mean validation, activation, controller application or external effects.

```go
type ReselectionResult struct {
	OperationID string
	Operation   api.Operation
	Attempt     *api.CollectionReselectionAttempt
	Complete    bool
}
```

### ResolveReference

ResolveReference resolves an omitted, authorized live dependency to its opaque
resource version. It must return an error for a missing or unauthorized object.
Versions are observations, not locks: server preflight and activation CAS are
still mandatory. Nil resolves against the submitted collection only.

```go
type ResolveReference func(context.Context, Reference) (string, error)
```

### ResourceSource

ResourceSource supplies typed API resources without constructing a fleet-sized
slice. io.EOF ends the source. Resource values are serialized before advancing.

```go
type ResourceSource interface {
	Next(context.Context) (api.Resource, error)
}
```

### Result

Result retains the server-issued handle on partial progress or an error. It
never treats a stopped client as a request to undo or cancel server commits.

```go
type Result struct {
	OperationID string
	Operation   api.Operation
	Preflight   *api.Preflight
	Validation  *api.ValidationResultSummary
	Noop        bool
}
```

### Resume

Resume explicitly resumes the original operation only when frozen content
matches it. Identical item uploads are replay-safe by the server contract;
unknown transport outcomes are returned, never automatically retried here.

```go
func Resume(ctx context.Context, operations Operations, id string, frozen *Frozen) (Result, error)
```

### Slice

Slice adapts a small caller-owned typed collection. FreezeResources copies its
serialized values; callers must not mutate the slice during freezing.

```go
func Slice(resources []api.Resource) ResourceSource
```

### Source

Source identifies a file, directory, URL, or caller-owned reader. Constructors
keep source fetching separate from the authenticated CPRa client.

```go
type Source struct {
	// contains filtered or unexported fields
}
```

### URL

URL selects an explicit remote input for Freeze's separate source client.
CPRa authentication is never inherited by that client.

```go
func URL(address string) Source
```

### ValidateFileProfileResource

ValidateFileProfileResource checks whether a decoded resource belongs to the
named file normalization profile. It neither serializes nor changes the
resource and does not validate its schema, references or provider settings.
Callers must separately validate the resource before admission. Empty and
unknown profiles return ErrUnsupportedNormalization; callers preserving an
unprofiled input contract must explicitly omit this check for that contract.

```go
func ValidateFileProfileResource(profile string, resource api.Resource) error
```

### ValidateReferences

ValidateReferences checks all shared definitions, not only referenced groups.
It returns observed versions for dependencies omitted from the collection.
Provider capabilities and permission checks remain server responsibilities.

```go
func ValidateReferences(ctx context.Context, frozen *Frozen, resolve ResolveReference) (map[Reference]string, error)
```

### Wait

Wait waits for the original collection's retained execution result. Apply
itself returns on activation admission. The returned Result keeps the last
verified observation, including partial counts, when waiting fails or stops.
A ready partial or failed result is returned without a transport error; its
immutable summary describes the execution outcome. Only one first page is
returned. The Frozen can be closed before Wait, and context cancellation never
cancels or resubmits server work.

```go
func Wait(ctx context.Context, operations ExecutionOperations, result Result) (Result, error)
```

## Package collection/commitment

### *Accumulator.Add

Add appends exactly the next ordinal. The stage, not this accumulator, must
reject repeated resource IDs at different ordinals. An error invalidates this
accumulator, so a caller cannot accidentally finalize a partially checked set.

```go
func (a *Accumulator) Add(position Position, itemMAC [MACBytes]byte) error
```

### *Accumulator.Close

Close discards the accumulator's MAC references. It is safe to defer and call
repeatedly, including after Finish. It does not promise erasure of Go memory.

```go
func (a *Accumulator) Close() error
```

### *Accumulator.Finish

Finish succeeds once, only after the declared count. A premature attempt
invalidates the accumulator. Callers retain the returned digest themselves.

```go
func (a *Accumulator) Finish() ([MACBytes]byte, error)
```

### *Accumulator.Verify

Verify finalizes this accumulator and checks the expected inventory MAC.

```go
func (a *Accumulator) Verify(expected []byte) error
```

### *SourceAccumulator.Begin

Begin opens the next source. Tokens must exactly match SourceToken(1), (2),
and so on; no identity set is needed to detect duplicate/reordered tokens.

```go
func (s *SourceAccumulator) Begin(token string) error
```

### *SourceAccumulator.Close

Close clears the owned raw key and drops MAC references, including an open
source. It is safe to defer or call repeatedly. Go/HMAC copies cannot be
guaranteed erased from memory.

```go
func (s *SourceAccumulator) Close() error
```

### *SourceAccumulator.End

End closes a source. Its byte length and keyed source MAC enter the outer
fingerprint as separate length-framed fields, so concatenation is unambiguous.

```go
func (s *SourceAccumulator) End() error
```

### *SourceAccumulator.Finish

Finish succeeds once after all sources are closed. It clears the explicitly
retained key buffer but cannot guarantee erasure of HMAC/runtime key copies.

```go
func (s *SourceAccumulator) Finish() ([MACBytes]byte, error)
```

### *SourceAccumulator.Write

Write consumes part of the open source. Chunk boundaries do not affect the
fingerprint. A quota failure consumes no bytes and invalidates the accumulator.

```go
func (s *SourceAccumulator) Write(data []byte) (int, error)
```

### Accumulator.Format

```go
func (a Accumulator) Format(w fmt.State, _ rune)
```

### Accumulator.GoString

```go
func (a Accumulator) GoString() string
```

### Accumulator.MarshalJSON

```go
func (Accumulator) MarshalJSON() ([]byte, error)
```

### Accumulator.String

```go
func (Accumulator) String() string
```

### Accumulator

Accumulator retains constant-size MAC state, never a resource identity index.
The zero value is invalid. Use one owner; do not copy an initialized value.

```go
type Accumulator struct {
	// contains filtered or unexported fields
}
```

### ErrKey ErrBounds ErrPosition ErrState ErrMismatch ErrSerialization

```go
var (
	ErrKey           = errors.New("inventory requires a 32-byte key")
	ErrBounds        = errors.New("inventory limit exceeded or invalid count")
	ErrPosition      = errors.New("invalid inventory identity or position")
	ErrState         = errors.New("invalid inventory accumulator state or order")
	ErrMismatch      = errors.New("inventory commitment does not match")
	ErrSerialization = errors.New("private inventory accumulators cannot be serialized")
)
```

### Format KeyBytes MACBytes MaxResourceBytes MaxItems MaxSources MaxSourceBytes

```go
const (
	Format                  = "cpra.collection.hmac-sha256-json-bytes.v1"
	KeyBytes                = 32
	MACBytes                = sha256.Size
	MaxResourceBytes        = 1 << 20
	MaxItems         uint64 = 10_000_000
	MaxSources       uint64 = 1_000_000
	MaxSourceBytes   uint64 = 1 << 40
)
```

### ItemMAC

ItemMAC commits to the exact transmitted resource-object bytes. It deliberately
does not parse or re-encode JSON. Receivers verify before separately decoding
and validating resource identity and semantics.

```go
func ItemMAC(key []byte, position Position, resourceJSON []byte) ([MACBytes]byte, error)
```

### NewAccumulator

NewAccumulator creates an ordered item inventory, including a private source
fingerprint. An empty inventory is well-defined for local no-op identities.

```go
func NewAccumulator(key []byte, itemCount uint64, sourceFingerprint [MACBytes]byte) (*Accumulator, error)
```

### NewSourceAccumulator

NewSourceAccumulator accepts a source count and explicit total byte quota.
Zero sources and a zero quota are valid for an empty local collection; nonempty
sources may be empty files. No resource or source randomness is generated here.

```go
func NewSourceAccumulator(key []byte, sourceCount, maxBytes uint64) (*SourceAccumulator, error)
```

### Position

Position identifies one input item. ID is Kind/ID; Ordinal is one-based.

```go
type Position struct {
	Ordinal uint64
	ID      string
	Source  SourcePosition
}
```

### SourceAccumulator.Format

```go
func (s SourceAccumulator) Format(w fmt.State, _ rune)
```

### SourceAccumulator.GoString

```go
func (s SourceAccumulator) GoString() string
```

### SourceAccumulator.MarshalJSON

```go
func (SourceAccumulator) MarshalJSON() ([]byte, error)
```

### SourceAccumulator.String

```go
func (SourceAccumulator) String() string
```

### SourceAccumulator

SourceAccumulator fingerprints ordered raw sources incrementally. It keeps
neither paths nor source bodies. Begin/Write/End define unambiguous boundaries.
Its byte quota covers all source bytes, including an open source. The zero value
is invalid. It is single-owner and must not be copied after initialization.

```go
type SourceAccumulator struct {
	// contains filtered or unexported fields
}
```

### SourcePosition

SourcePosition identifies a location without disclosing the local source name.
Document and Item are one-based and bounded by MaxItems.

```go
type SourcePosition struct {
	Token    string
	Document uint64
	Item     uint64
}
```

### SourceToken

SourceToken returns the sole canonical spelling of an ordered opaque token.

```go
func SourceToken(ordinal uint64) (string, error)
```

### VerifyItem

VerifyItem compares a recomputed item MAC without a data-dependent comparison.
It reports no input material in its errors.

```go
func VerifyItem(key []byte, position Position, resourceJSON, expected []byte) error
```

## Package worker

### *Registry.Register

Build requirement: `externaljobs`.

Register associates an exact ID, version, and category with a handler.
It rejects invalid identifiers/categories, duplicates, more than 64 capabilities,
and changes after Run has started.

```go
func (r *Registry) Register(jobTypeID, version, kind string, handler Handler) error
```

### *Runner.Close

Build requirement: `externaljobs`.

Close releases an unused or finished runner's journal. It returns ErrRunning
while Run still owns handlers or protocol calls; cancel Run's context first.

```go
func (r *Runner) Close() error
```

### *Runner.QueueLateEvidence

Build requirement: `externaljobs`.

QueueLateEvidence durably queues one append-only evidence submission for an
unknown execution with a confirmed original receipt. The caller supplies a
stable fresh EvidenceID and may safely retry identical evidence. It neither
replaces the original result nor re-enables execution.

```go
func (r *Runner) QueueLateEvidence(ctx context.Context, evidence api.LateEvidenceRequest) error
```

### *Runner.Run

Build requirement: `externaljobs`.

Run stops admission on cancellation, then requests cooperative cancellation.
Drain expiry is immediately visible through Status. It does not release the
lock or return while a handler remains alive; process supervision is required
for a hard execution deadline.

```go
func (r *Runner) Run(ctx context.Context) (runErr error)
```

### *Runner.Status

Build requirement: `externaljobs`.

Status returns current admission, handler, and journal accounting. After Run
returns it reports the final retained status without reopening the journal.

```go
func (r *Runner) Status() (Status, error)
```

### Config

Build requirement: `externaljobs`.

Config requires absolute state/key paths, a private 32-byte key file outside
StateDir, and the expected CPRa store/restore identity. No key is generated or
replaced automatically. Use a separate state directory for each worker.

```go
type Config struct {
	Client            Protocol
	Registry          *Registry
	WorkerID          string
	WorkerUID         string // Immutable provisioned worker incarnation; required on every restart.
	ServerID          string
	StateDir          string
	WrappingKeyPath   string
	Credentials       CredentialResolver
	Limits            Limits
	DrainTimeout      time.Duration
	RetryInterval     time.Duration
	PollWait          time.Duration
	HeartbeatInterval time.Duration
}
```

### CredentialResolver

Build requirement: `externaljobs`.

CredentialResolver resolves an opaque profile within the worker. Resolved
values are never persisted by this library or requested from CPRa.

```go
type CredentialResolver func(context.Context, string) (any, error)
```

### ErrCapacity ErrCorrupt ErrIdentity ErrRunning ErrDrainDeadline ErrClosed ErrDuplicate ErrStorage ErrJournalVersion

Build requirement: `externaljobs`.

```go
var (
	ErrCapacity       = errors.New("worker journal admission capacity exhausted")
	ErrCorrupt        = errors.New("worker journal is corrupt or cannot be decrypted")
	ErrIdentity       = errors.New("worker server or journal identity mismatch")
	ErrRunning        = errors.New("worker is already running or has run")
	ErrDrainDeadline  = errors.New("worker drain deadline expired; handler still active")
	ErrClosed         = errors.New("worker journal is closed")
	ErrDuplicate      = errors.New("execution already recorded")
	ErrStorage        = errors.New("worker journal durable write failed")
	ErrJournalVersion = errors.New("unsupported worker journal format")
)
```

### Handler

Build requirement: `externaljobs`.

Handler returns the server-owned outcome envelope. Execution identity, grant,
and kind are filled by the runner, never taken from handler output. Errors are
intentionally not serialized: they may contain provider credentials.

```go
type Handler func(context.Context, Job) (api.Outcome, error)
```

### Job

Build requirement: `externaljobs`.

Job contains a copied assignment and worker-local credential resolution.
Do not include credentials in returned diagnostics, data, or evidence.

```go
type Job struct {
	Assignment  api.Assignment
	Credentials any
}
```

### Limits

Build requirement: `externaljobs`.

Limits bound admission before execution permission is requested. Byte counts
distinguish reserved payload capacity, live encrypted-record plaintext, and
the allocated bbolt file. Allocation limits do not interrupt completion writes.

```go
type Limits struct {
	Concurrency     int
	Records         int
	LiveBytes       int64
	OutcomeBytes    int
	DiagnosticBytes int
	AllocatedBytes  int64
}
```

### New

Build requirement: `externaljobs`.

New validates configuration and opens the exclusively locked encrypted
journal. It starts no poller and invokes no handler until Run is called.

```go
func New(config Config) (*Runner, error)
```

### NewRegistry

Build requirement: `externaljobs`.

NewRegistry constructs an empty registry for locally compiled handlers.

```go
func NewRegistry() *Registry
```

### Protocol

Build requirement: `externaljobs`.

Protocol is implemented by the SDK WorkerClient. Implementations must honor
context cancellation and the server's durable start/receipt contracts.

```go
type Protocol interface {
	Poll(context.Context, api.PollRequest) (*api.Assignments, error)
	Start(context.Context, api.StartRequest) (*api.StartResponse, error)
	Heartbeat(context.Context, api.HeartbeatRequest) (*api.HeartbeatResponse, error)
	Result(context.Context, api.Outcome) (*api.Receipt, error)
	LateEvidence(context.Context, api.LateEvidenceRequest) (*api.Receipt, error)
}
```

### Registry

Build requirement: `externaljobs`.

Registry maps exact immutable JobType versions to locally compiled handlers.
It becomes immutable when a Runner using it starts.

```go
type Registry struct {
	// contains filtered or unexported fields
}
```

### Runner

Build requirement: `externaljobs`.

Runner owns a journal and one execution slot per accepted assignment. Run is
single-use. Close releases an unused runner; a running runner closes only once
all handler goroutines and protocol calls have returned.

```go
type Runner struct {
	// contains filtered or unexported fields
}
```

### Status

Build requirement: `externaljobs`.

Status reports bounded worker activity and journal usage. Allocated bytes can
exceed live bytes because removing records does not shrink the database file.

```go
type Status struct {
	Running              bool
	Draining             bool
	DrainDeadlineExpired bool
	ActiveHandlers       int
	PendingOutcomes      int
	UnknownActions       int
	Records              int
	AllocatedBytes       int64
	LiveBytes            int64
	ReservedBytes        int64
	AdmissionAvailable   bool
	AdmissionCapacity    int
	LastError            error
}
```

